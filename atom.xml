<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>zylyye - notebook</title>
  
  <subtitle>热爱可抵岁月漫长</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://zylyye.cn/"/>
  <updated>2021-02-04T03:23:09.870Z</updated>
  <id>http://zylyye.cn/</id>
  
  <author>
    <name>zylyye</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>变量</title>
    <link href="http://zylyye.cn/2021/02/02/bash/05-variable/"/>
    <id>http://zylyye.cn/2021/02/02/bash/05-variable/</id>
    <published>2021-02-02T07:27:09.000Z</published>
    <updated>2021-02-04T03:23:09.870Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><hr><p>Bash 中有两类变量：</p><ul><li><strong>环境变量</strong></li><li><strong>自定义变量</strong></li></ul><a id="more"></a><h3 id="环境变量"><a href="#环境变量" class="headerlink" title="环境变量"></a>环境变量</h3><p>环境变量可以通过 <code>env</code> 或者 <code>printenv</code> 命令查看所有的环境变量。</p><p><strong>常见的环境变量：</strong></p><ul><li><p><code>BASHPID</code>： Bash 进程ID</p></li><li><p><code>BASHOPTS</code>:  当前 Shell 的参数，可由 <code>shopt</code>命令修改</p></li><li><p><code>DISPLAY</code>:  图形环境显示器的名字，<code>:0</code> 表示第一个显示器</p></li><li><p><code>EDITOR</code>: 默认文本编辑器</p></li><li><p><code>HOME</code> ：用户主目录</p></li><li><p><code>IFS</code>：词之间分隔符，默认空格</p></li><li><p><code>LANG</code>：字符集以及语言编码，例如 <code>zh_CN.UTF-8</code></p></li><li><p><code>PATH</code>：可执行程序搜索目录列表</p></li><li><p><code>PS1</code>: Shell 提示符</p></li><li><p><code>PS2</code>: 输入多行命令，Shell 次要提示符</p></li><li><p><code>PWD</code>： 当前工作目录</p></li><li><p><code>RANDOM</code>： 返回 0 ~ 32767 之间的随机数</p></li><li><p><code>SHELL</code>：Shell 的名字</p></li><li><p><code>SHELLOPTS</code>:  启动当前 Shell 的 <code>set</code>  命令的参数</p></li><li><p><code>TERM</code>:   当前终端类型名</p></li><li><p><code>UID</code>： 当前用户的 ID 编号</p></li><li><p><code>USER</code>：当前用户的用户名</p></li></ul><p>查看单个变量名可使用 <code>printenv</code> 或 <code>echo</code></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">printenv PATH</span><br><span class="line">echo $PATH</span><br></pre></td></tr></table></figure><p>​                                                                                                                    </p><h3 id="自定义变量"><a href="#自定义变量" class="headerlink" title="自定义变量"></a>自定义变量</h3><p><strong>自定义变量只在当前 Shell 环境下有效</strong></p><p>可以使用 <code>set</code> 查看当前 Shell 中所有的<strong>环境变量</strong>和<strong>自定义变量</strong>。</p><h2 id="创建变量"><a href="#创建变量" class="headerlink" title="创建变量"></a>创建变量</h2><hr><p>变量的命名规则：</p><ul><li>字母，数字和下划线字符组成</li><li>第一个字符必须是字母或下划线</li><li>不能有空格或标点符号</li></ul><p>变量声明时，等号两边<strong>不能空格</strong>：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">var_name=var_value</span><br></pre></td></tr></table></figure><ul><li>Bash 变量只有字符串类型</li><li>变量可以重复赋值</li></ul><h2 id="变量读取"><a href="#变量读取" class="headerlink" title="变量读取"></a>变量读取</h2><hr><p>使用 <code>$</code> 符号读取变量：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">echo $var_name</span><br><span class="line"><span class="meta">#</span><span class="bash"> 或</span></span><br><span class="line">echo $&#123;var_name&#125;</span><br></pre></td></tr></table></figure><ul><li>如果变量的值是变量，可以使用 <code>${!var_name}</code> 读取</li></ul><h2 id="删除变量"><a href="#删除变量" class="headerlink" title="删除变量"></a>删除变量</h2><hr><p>使用 <code>unset</code>可以删除一个变量。</p><hr><h2 id="输出变量"><a href="#输出变量" class="headerlink" title="输出变量"></a>输出变量</h2><hr><p>自定义变量仅在当前 Shell 下有效，子 Shell 读取不到，可以使用 <code>export</code> 来使改变量能被子 Shell 访问。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">export a=1</span><br><span class="line"><span class="meta">#</span><span class="bash"> 或者分两步执行</span></span><br><span class="line">a=1</span><br><span class="line">export a</span><br></pre></td></tr></table></figure><ul><li>注意在子 Shell 中修改 export 的变量，不会影响到父 Shell 的值</li></ul><h2 id="特殊变量"><a href="#特殊变量" class="headerlink" title="特殊变量"></a>特殊变量</h2><hr><ul><li><code>$?</code> ： 上个命令的退出状态码，一般成功返回 0</li><li><code>$$</code>： 当前 Shell 的进程ID，例如可以用来命名临时文件<code>touch file.$$</code></li><li><code>$_</code>： 上个命令的最后一个参数</li><li><code>$!</code>： 最近一个后台执行的命令的进程 ID</li><li><code>$0</code>： 当前 Shell 名称或脚本名</li><li><code>$-</code>： 当前 Shell 的启动参数</li><li><code>$@</code> 和 <code>$#</code>：表示脚本的参数数量</li></ul><h2 id="变量的默认值"><a href="#变量的默认值" class="headerlink" title="变量的默认值"></a>变量的默认值</h2><hr><ul><li><code>${varname:-word}</code>:  （<strong>返回默认值</strong>）如果变量不存在或为空，则返回 word，否则返回变量的值</li><li><code>${varname:=word}</code>：（<strong>设置默认值</strong>）如果变量不存在或为空，则被设置并返回 word，否则返回变量的值</li><li><code>${varname:+word}</code>:  （<strong>存在时返回设置值</strong>）用来测试变量是否存在，如果存在且不会空则返回word，否则返回空值</li><li><code>${varname:?message}</code>： （<strong>不存在则退出并提示指定信息</strong>）如果变量不存在或为空，则打印 <code>varname: message</code>，且中断脚本执行，用来防止变量未定义。</li><li><code>${1:?&#39;变量不存在&#39;}</code>： 变量名可以使用 1~9，表示脚本的参数</li></ul><h2 id="declare-命令"><a href="#declare-命令" class="headerlink" title="declare 命令"></a>declare 命令</h2><hr><p>declare 命令用来声明变量，可以对变量增加一些特性限制。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">declare OPTIONS VARIABLE=value</span><br></pre></td></tr></table></figure><p><code>declare</code> 在函数内部声明的变量只在函数内部有效，等同于 <code>local</code>命令，<code>declare</code> 命令默认合宿出当前环境的所有变量，包括函数在内，等同于不带任何参数的 <code>set</code></p><p>OPTIONS 可选值：</p><ul><li><code>-a</code>:  声明数组变量</li><li><code>-f</code>：输出所有函数定义</li><li><code>-F</code>：输出所有函数名</li><li><code>-i</code>：声明整数变量</li></ul><p>声明的整数变量，可直接进行数学运算</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">declare -i num1=1 num2=10</span><br><span class="line"><span class="meta">#</span><span class="bash"> 只要将 sum 声明为整数变量，就会进行整数运算</span></span><br><span class="line">declare -i sum</span><br><span class="line">sum=num1+num2</span><br><span class="line">echo $sum // 输出 11</span><br></pre></td></tr></table></figure><ul><li><code>-x</code>：声明为环境变量，等同于 export</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">declare -x a=1122</span><br><span class="line"><span class="meta">#</span><span class="bash"> 等价与</span></span><br><span class="line">export a=1122</span><br></pre></td></tr></table></figure><ul><li><p><code>-r</code>：声明为只读变量，不能更改和 <code>unset</code></p></li><li><p><code>-u</code>：声明变量为大写字母，会自动将变量的值转化为大写字母</p></li><li><p><code>-l</code>：声明变量为小写字母，会自动将变量的值转化为小写字母</p></li><li><p><code>-p</code>： 输出变量信息</p></li></ul><h2 id="readonly-命令"><a href="#readonly-命令" class="headerlink" title="readonly 命令"></a><code>readonly</code> 命令</h2><hr><p><code>readonly</code>命令等价于 <code>declare -r</code>，用于声明只读变量。</p><p><code>readonly</code> 有三个参数：</p><ul><li><code>-f</code>： 声明的变量为函数名</li><li><code>-p</code>：打印出所有的只读变量</li><li><code>-a</code>：声明的变量为数组</li></ul><h2 id="let-命令"><a href="#let-命令" class="headerlink" title="let 命令"></a>let 命令</h2><hr><p>使用 <code>let</code> 命令声明变量可以直接进行算术计算：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">let a=1+2</span><br><span class="line">let b=a++</span><br></pre></td></tr></table></figure><p>如果有空格，需要使用引号包含整个表达式</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">let "a = 1 + 2"</span><br></pre></td></tr></table></figure><p>多个使用空格隔开</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">let c=1 d=2</span><br><span class="line">let "c = 1" "d = 2"</span><br></pre></td></tr></table></figure><hr>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;Bash 中有两类变量：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;环境变量&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;自定义变量&lt;/strong&gt;&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="bash" scheme="http://zylyye.cn/categories/bash/"/>
    
    
      <category term="linux" scheme="http://zylyye.cn/tags/linux/"/>
    
      <category term="bash" scheme="http://zylyye.cn/tags/bash/"/>
    
  </entry>
  
  <entry>
    <title>引号和转义</title>
    <link href="http://zylyye.cn/2021/01/29/bash/04-quotation/"/>
    <id>http://zylyye.cn/2021/01/29/bash/04-quotation/</id>
    <published>2021-01-29T10:04:25.000Z</published>
    <updated>2021-02-04T03:22:35.523Z</updated>
    
    <content type="html"><![CDATA[<h2 id="转义"><a href="#转义" class="headerlink" title="转义"></a>转义</h2><hr><p>Bash 接收的数据都视为字符串类型，遇到特殊含义的字符需要使用反斜杠转义：\</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">echo \$abc</span><br><span class="line">echo \\</span><br></pre></td></tr></table></figure><a id="more"></a><h2 id="单引号与双引号"><a href="#单引号与双引号" class="headerlink" title="单引号与双引号"></a>单引号与双引号</h2><hr><p>字符串可以放到单引号或双引号中，单引号中的<strong>特殊字符不需要转义</strong>，双引号中的部分特殊字符<strong>需要转义</strong>(<figure class="highlight plain"><figcaption><span>` $```)。</span></figcaption><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">- **双引号中可以收入多行文本**：</span><br><span class="line"></span><br><span class="line">```shell</span><br><span class="line">echo &quot;one</span><br><span class="line"> two three</span><br><span class="line"> four&quot;</span><br></pre></td></tr></table></figure></p><ul><li><strong>双引号可以保存命令输出的原始格式</strong></li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">echo "$(cal)"</span><br></pre></td></tr></table></figure><h2 id="Here-文档"><a href="#Here-文档" class="headerlink" title="Here 文档"></a>Here 文档</h2><hr><p>Here 是一种输入多行字符串的方法，格式如下</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&lt;&lt; end_tag_name</span><br><span class="line">&lt;一些文本&gt;</span><br><span class="line">end_tag_name</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">cat &lt;&lt; _EOF_</span><br><span class="line">This is a</span><br><span class="line">test text for</span><br><span class="line">here document.</span><br><span class="line">_EOF_</span><br></pre></td></tr></table></figure><ul><li>Here 文档中的变量会发生替换，单/双引号原样输出</li><li>如果将开始标记放到单引号中，则内容中的变量不会发生替换</li><li>Here 文档的语法<strong>本质是重定向</strong>，类似 <code>echo test &gt;&gt; demo.txt</code>，因此只适合接收标准输入的命令</li></ul><h2 id="Here-字符串"><a href="#Here-字符串" class="headerlink" title="Here 字符串"></a>Here 字符串</h2><hr><p>与 Here 文档类似，Here 字符串也用于重定向字符串到标准输入，通常用于简单的字符串文本：</p><p><code>cat &lt;&lt;&lt; &#39;good job!&#39;</code></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;转义&quot;&gt;&lt;a href=&quot;#转义&quot; class=&quot;headerlink&quot; title=&quot;转义&quot;&gt;&lt;/a&gt;转义&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;Bash 接收的数据都视为字符串类型，遇到特殊含义的字符需要使用反斜杠转义：\&lt;/p&gt;
&lt;figure class=&quot;highlight shell&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;echo \$abc&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;echo \\&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="bash" scheme="http://zylyye.cn/categories/bash/"/>
    
    
      <category term="linux" scheme="http://zylyye.cn/tags/linux/"/>
    
      <category term="bash" scheme="http://zylyye.cn/tags/bash/"/>
    
  </entry>
  
  <entry>
    <title>模式扩展</title>
    <link href="http://zylyye.cn/2021/01/28/bash/03-expansion/"/>
    <id>http://zylyye.cn/2021/01/28/bash/03-expansion/</id>
    <published>2021-01-28T09:50:46.000Z</published>
    <updated>2021-02-04T03:22:06.539Z</updated>
    
    <content type="html"><![CDATA[<h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><hr><p>模式扩展(globbing)，Bash 中提供了八种扩展</p><ul><li>波浪线扩展</li><li><code>?</code> 字符扩展</li><li><code>*</code> 字符扩展</li><li>方括号扩展</li><li>大括号扩展</li><li>变量扩展</li><li>子命令扩展</li><li>算术扩展</li></ul><a id="more"></a><h2 id="开启-关闭扩展"><a href="#开启-关闭扩展" class="headerlink" title="开启/关闭扩展"></a>开启/关闭扩展</h2><hr><ul><li>开启 <code>set -o noglob</code> 或 <code>set -f</code></li><li>关闭 <code>set +o noglog</code> 或 <code>set +f</code></li></ul><h2 id="波浪线扩展"><a href="#波浪线扩展" class="headerlink" title="波浪线扩展"></a>波浪线扩展</h2><hr><p><code>～</code> 会自动扩展为用户主目录，<code>~user</code> 会扩展为<strong>指定用户的主目录</strong></p><p>例如：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">echo ~</span><br><span class="line">cd ~/.local</span><br><span class="line">echo ~zylyye</span><br></pre></td></tr></table></figure><h2 id="？-字符扩展"><a href="#？-字符扩展" class="headerlink" title="？ 字符扩展"></a><code>？</code> 字符扩展</h2><hr><p><code>？</code> 字符代表单个字符（不包括空字符）</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ls abc??.txt</span><br></pre></td></tr></table></figure><p>只有文件存在时才会发生扩展，如果没有文件匹配，则会原样输出。</p><h2 id="字符扩展"><a href="#字符扩展" class="headerlink" title="*字符扩展"></a><code>*</code>字符扩展</h2><hr><p><code>*</code>匹配任意数量的任意字符，包括<strong>零个字符</strong></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ls *.md</span><br><span class="line">echo *.md</span><br></pre></td></tr></table></figure><p>与 <code>？</code> 类似，如果没有文件匹配，则会原样输出。</p><p><code>*</code> 只匹配当前目录，如需匹配子目录，<strong>每个层级都需要一个 <code>*</code> 号</strong>，如 <code>*/*/*/*.txt</code></p><p>Bash 4.0 后引入 globstar，可以使用 <code>**</code>匹配零个或多个子目录。例如 <code>**/*.txt</code></p><h2 id="方括号扩展"><a href="#方括号扩展" class="headerlink" title="方括号扩展"></a>方括号扩展</h2><hr><p><code>[abcd]</code> 方括号会匹配括号中的任意字符，这里匹配 a/b/c/d</p><p>可以使用 <code>[^...]</code> 或 <code>[!...]</code> 来匹配除了括号中的字符</p><p>[a-z] 相当于 [abcdef…z]</p><ul><li>如需匹配 <code>-</code> 需放在开头或结尾 [-123] 或 [123-]</li><li><code>[</code> 或 <code>]</code> 也可放在方括号中</li></ul><h2 id="大括号扩展"><a href="#大括号扩展" class="headerlink" title="大括号扩展"></a>大括号扩展</h2><hr><p>大括号 <code>{a,b,c}</code>会挨个展开括号中逗号隔开的值，如 <code>{a,b,c}.txt</code> 展开为 a.txt b.txt c.txt。</p><ul><li>注意，<strong>括号中逗号前后不能有空格</strong>，否则会扩展失效</li><li>括号可以嵌套 <code>{a,b{1,2},c}</code></li><li>可以使用形如 <code>{a..z}</code> 进行连续扩展，相当于 <code>{a,b,c,d...,z}</code></li></ul><p><strong>可以结合 for 循环使用</strong>：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">for i in &#123;0..9&#125;</span><br><span class="line">do</span><br><span class="line">echo $i</span><br><span class="line">done</span><br></pre></td></tr></table></figure><p><strong>可以指定扩展的前缀</strong>：</p><p>{01-9} 扩展为 01 02 03 04 …. 09</p><p>可以指定步长 {start..end..step}</p><p>例如 {0..8..2} 扩展为 0 2 4 6 8</p><p><strong>连续多个形成笛卡尔积</strong>：</p><p>{a..c}{1..3} 扩展为 a1 a2 a3 b1 b2 b3 c1 c2 c3</p><h2 id="变量扩展"><a href="#变量扩展" class="headerlink" title="变量扩展"></a>变量扩展</h2><hr><p><code>$</code> 可用来展开变量，获取变量的值，例如 <code>$SHELL</code> 即展开变量 <code>SHELL</code> 的值。</p><ul><li><p>变量也可放在花括号中，例如<code>${SEHLL}</code></p></li><li><p>可使用 <code>${!string*}</code> 或 <code>${!string@}</code>匹配以 <code>string</code> 开头的变量名</p></li></ul><h2 id="子命令扩展"><a href="#子命令扩展" class="headerlink" title="子命令扩展"></a>子命令扩展</h2><hr><p><code>$(command)</code> 可以扩展命令运行的结果。</p><ul><li>比较老的语法:</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">`command`</span><br><span class="line"><span class="meta">#</span><span class="bash"> 例如</span></span><br><span class="line">echo `date`</span><br></pre></td></tr></table></figure><ul><li>该语法可以嵌套，例如 <code>$(ls $(pwd))</code></li></ul><h2 id="算术扩展"><a href="#算术扩展" class="headerlink" title="算术扩展"></a>算术扩展</h2><hr><p>使用 <code>$((...))</code> 进行算术运算</p><p>例如 <code>echo $((2 + 4))</code></p><h2 id="字符类"><a href="#字符类" class="headerlink" title="字符类"></a>字符类</h2><hr><p><code>[[:class:]]</code> 用来表示某个字符类，可以匹配某一类字符。</p><ul><li><code>[[:alnum:]]</code>：匹配任意英文字母与数字</li><li><code>[[:alpha:]]</code>：匹配任意英文字母</li><li><code>[[:blank:]]</code>：空格和 Tab 键。</li><li><code>[[:cntrl:]]</code>：ASCII 码 0-31 的不可打印字符。</li><li><code>[[:digit:]]</code>：匹配任意数字 0-9。</li><li><code>[[:graph:]]</code>：A-Z、a-z、0-9 和标点符号。</li><li><code>[[:lower:]]</code>：匹配任意小写字母 a-z。</li><li><code>[[:print:]]</code>：ASCII 码 32-127 的可打印字符。</li><li><code>[[:punct:]]</code>：标点符号（除了 A-Z、a-z、0-9 的可打印字符）。</li><li><code>[[:space:]]</code>：空格、Tab、LF（10）、VT（11）、FF（12）、CR（13）。</li><li><code>[[:upper:]]</code>：匹配任意大写字母 A-Z。</li><li><code>[[:xdigit:]]</code>：16进制字符（A-F、a-f、0-9）。</li></ul><h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><hr><ul><li>Bash 会先进行模式扩展，再执行命令</li><li>文件名扩展如果没有匹配到文件，扩展会原样输出</li><li>多层路径需要手动添加 / ，Bash 4.0 增加 <code>**</code> 用来匹配零个或多个子路径  </li><li>创建的文件名中可以包含通配符（需使用单引号包裹）</li></ul><h2 id="量词语法"><a href="#量词语法" class="headerlink" title="量词语法"></a>量词语法</h2><hr><p>量词语法可以指定模式匹配的次数，可以使用一下命令启用或禁用该特性：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 查询开启状态</span></span><br><span class="line">shopt extglob</span><br><span class="line"><span class="meta">#</span><span class="bash"> 启用</span></span><br><span class="line">shopt -s extglob</span><br><span class="line"><span class="meta">#</span><span class="bash"> 禁用</span></span><br><span class="line">shopt -u extglob</span><br></pre></td></tr></table></figure><p>量词语法的集中模式</p><ul><li><code>?(pattern-list)</code>: 匹配零个或一个模式</li><li><code>*(pattern-list)</code>: 匹配零个或多个模式</li><li><code>+(pattern-list)</code>: 匹配一个或多个模式</li><li><code>@(pattern-list)</code>: 只匹配一个模式</li><li><code>!(pattern-list)</code>: 匹配指定模式之外的内容</li></ul><p>例如：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 匹配零个或一个 def</span></span><br><span class="line">?(def)</span><br><span class="line"><span class="meta">#</span><span class="bash"> 匹配一个或多个 .txt 或 .php</span></span><br><span class="line">+(.txt|.php)</span><br></pre></td></tr></table></figure><h2 id="shopt-命令"><a href="#shopt-命令" class="headerlink" title="shopt 命令"></a>shopt 命令</h2><hr><p>shopt 命令可以调整 Bash 的特性：</p><ul><li>开启 <code>shopt -s [option_name]</code></li><li>关闭 <code>shopt -u [option_name]</code></li><li>查询 <code>shopt [option_name]</code></li></ul><p><strong>option_name:</strong></p><ul><li><code>dotglob</code>    - 扩展结果包含隐藏文件（以 . 开头的文件）</li><li><code>nullglob</code>   - 不匹配任何文件时，返回空字符</li><li><code>failglob</code>    - 不匹配任何文件直接报错</li><li><code>extglob</code>   - 开启量词语法支持</li><li><code>nocaseglob</code>   - 通配符扩展不区分大小写</li><li><code>globstar</code>    - 支持 <code>**</code> 来匹配零个或多层目录</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;简介&quot;&gt;&lt;a href=&quot;#简介&quot; class=&quot;headerlink&quot; title=&quot;简介&quot;&gt;&lt;/a&gt;简介&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;模式扩展(globbing)，Bash 中提供了八种扩展&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;波浪线扩展&lt;/li&gt;
&lt;li&gt;&lt;code&gt;?&lt;/code&gt; 字符扩展&lt;/li&gt;
&lt;li&gt;&lt;code&gt;*&lt;/code&gt; 字符扩展&lt;/li&gt;
&lt;li&gt;方括号扩展&lt;/li&gt;
&lt;li&gt;大括号扩展&lt;/li&gt;
&lt;li&gt;变量扩展&lt;/li&gt;
&lt;li&gt;子命令扩展&lt;/li&gt;
&lt;li&gt;算术扩展&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="bash" scheme="http://zylyye.cn/categories/bash/"/>
    
    
      <category term="linux" scheme="http://zylyye.cn/tags/linux/"/>
    
      <category term="bash" scheme="http://zylyye.cn/tags/bash/"/>
    
  </entry>
  
  <entry>
    <title>基本语法</title>
    <link href="http://zylyye.cn/2021/01/27/bash/02-grammar/"/>
    <id>http://zylyye.cn/2021/01/27/bash/02-grammar/</id>
    <published>2021-01-27T09:06:00.000Z</published>
    <updated>2021-02-04T03:21:41.404Z</updated>
    
    <content type="html"><![CDATA[<h2 id="命令格式"><a href="#命令格式" class="headerlink" title="命令格式"></a>命令格式</h2><hr><p><code>command [arg1 ... [argN]]</code></p><ul><li>命令的参数使用空格隔开</li><li>如果命令过长，可以使用饭斜杠 \ 进行换行</li><li>一行可以放多个命令，多个命令使用分号 (;) 隔开</li><li>命令可以使用逻辑组合 &amp;&amp; 与 ||</li></ul><a id="more"></a><h2 id="echo-命令"><a href="#echo-命令" class="headerlink" title="echo 命令"></a>echo 命令</h2><hr><p><code>echo [content]</code></p><ul><li>-n 取消末尾换行</li><li>-e  启用转义字符 (注意 zsh 默认启用，bash 默认不启用，即 bash 中 \n 原字符输出，而不是换行)</li></ul><h2 id="type-命令"><a href="#type-命令" class="headerlink" title="type 命令"></a>type 命令</h2><hr><p><code>type command_name</code></p><p>Bash 中内置了很多命令，type 用来查询命令是内置命令还是外部命令</p><ul><li>-a 查看命令所有的定义</li><li>-t 查看命令类型  alias/keyword/function/builtin/file</li></ul><h2 id="快捷键"><a href="#快捷键" class="headerlink" title="快捷键"></a>快捷键</h2><hr><ul><li>Ctrl + L   清除屏幕</li><li>Ctrl + C   中止执行</li><li>Shift + PageUp 向上滚动</li><li>Shift + PageDown 向下滚动</li><li>Ctrl + U 删除到行首</li><li>Ctrl + K 删除到行尾</li><li>Ctrl + D 关闭 Shell 会话</li><li>Ctrl + A 移到行首</li><li>Ctrl + E 移到行尾</li><li>方向上/下 浏览执行历史</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;命令格式&quot;&gt;&lt;a href=&quot;#命令格式&quot; class=&quot;headerlink&quot; title=&quot;命令格式&quot;&gt;&lt;/a&gt;命令格式&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;&lt;code&gt;command [arg1 ... [argN]]&lt;/code&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;命令的参数使用空格隔开&lt;/li&gt;
&lt;li&gt;如果命令过长，可以使用饭斜杠 \ 进行换行&lt;/li&gt;
&lt;li&gt;一行可以放多个命令，多个命令使用分号 (;) 隔开&lt;/li&gt;
&lt;li&gt;命令可以使用逻辑组合 &amp;amp;&amp;amp; 与 ||&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="bash" scheme="http://zylyye.cn/categories/bash/"/>
    
    
      <category term="linux" scheme="http://zylyye.cn/tags/linux/"/>
    
      <category term="bash" scheme="http://zylyye.cn/tags/bash/"/>
    
  </entry>
  
  <entry>
    <title>简介</title>
    <link href="http://zylyye.cn/2021/01/27/bash/01-introduction/"/>
    <id>http://zylyye.cn/2021/01/27/bash/01-introduction/</id>
    <published>2021-01-27T08:18:30.000Z</published>
    <updated>2021-02-04T03:19:18.875Z</updated>
    
    <content type="html"><![CDATA[<h2 id="什么是-Shell"><a href="#什么是-Shell" class="headerlink" title="什么是 Shell"></a>什么是 Shell</h2><hr><p> shell 是一个提供命令行环境的程序。</p><a id="more"></a><h2 id="Shell-种类"><a href="#Shell-种类" class="headerlink" title="Shell 种类"></a>Shell 种类</h2><hr><ul><li><p>sh </p></li><li><p>bash</p></li><li><p>csh</p></li><li><p>tcsh</p></li><li><p>ksh</p></li><li><p>zsh</p></li><li><p>fish</p></li></ul><h2 id="查看系统的-Shell-信息"><a href="#查看系统的-Shell-信息" class="headerlink" title="查看系统的 Shell 信息"></a>查看系统的 Shell 信息</h2><hr><ul><li><p>查看当前运行的Shell：echo $SHELL</p></li><li><p>查看系统安装的所有 Shell： cat /etc/shells</p></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;什么是-Shell&quot;&gt;&lt;a href=&quot;#什么是-Shell&quot; class=&quot;headerlink&quot; title=&quot;什么是 Shell&quot;&gt;&lt;/a&gt;什么是 Shell&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt; shell 是一个提供命令行环境的程序。&lt;/p&gt;
    
    </summary>
    
      <category term="bash" scheme="http://zylyye.cn/categories/bash/"/>
    
    
      <category term="linux" scheme="http://zylyye.cn/tags/linux/"/>
    
      <category term="bash" scheme="http://zylyye.cn/tags/bash/"/>
    
  </entry>
  
  <entry>
    <title>LSB 与 MSB</title>
    <link href="http://zylyye.cn/2020/04/13/read-note/lsb-and-msb/"/>
    <id>http://zylyye.cn/2020/04/13/read-note/lsb-and-msb/</id>
    <published>2020-04-13T08:51:41.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="LSB"><a href="#LSB" class="headerlink" title="LSB"></a>LSB</h2><hr><p> LSB 即 <strong>least significant bit</strong>， 代表一串二进制数的最低位 (low-order bit)，例如：</p><p>1 0 0 1 0 1 0 <strong>1</strong></p><p>加粗的二进制数即为 LSB。</p><a id="more"></a><h2 id="LSB-与数字密文"><a href="#LSB-与数字密文" class="headerlink" title="LSB 与数字密文"></a>LSB 与数字密文</h2><hr><p>在一串用于表示颜色的二进制数中，低位改变不会带来肉眼可见的区别，因此可将信息存放在 LSB 及其相邻的二进制中进行传输，达到信息隐藏的效果。</p><h2 id="MSB"><a href="#MSB" class="headerlink" title="MSB"></a>MSB</h2><hr><p>MSB 即 <strong>most significant bit</strong>，代表一串二进制数的最高位（high-order bit)，例如：</p><p><strong>1</strong> 0 0 1 0 1 0 1</p><p>加粗的二进制数即为 MSB。</p><h2 id="MSB-与有符号数"><a href="#MSB-与有符号数" class="headerlink" title="MSB 与有符号数"></a>MSB 与有符号数</h2><hr><p>对于有符号数，MSB 可作为该数的符号位，MSB 为 <code>1</code> 时表示该数为正数，为 <code>0</code> 时表示该数为负数。</p><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><hr><ul><li><a href="https://en.wikipedia.org/wiki/Bit_numbering">维基百科：Bit bumbering</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;LSB&quot;&gt;&lt;a href=&quot;#LSB&quot; class=&quot;headerlink&quot; title=&quot;LSB&quot;&gt;&lt;/a&gt;LSB&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt; LSB 即 &lt;strong&gt;least significant bit&lt;/strong&gt;， 代表一串二进制数的最低位 (low-order bit)，例如：&lt;/p&gt;
&lt;p&gt;1 0 0 1 0 1 0 &lt;strong&gt;1&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;加粗的二进制数即为 LSB。&lt;/p&gt;
    
    </summary>
    
      <category term="阅读笔记" scheme="http://zylyye.cn/categories/%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="LSB" scheme="http://zylyye.cn/tags/LSB/"/>
    
      <category term="MSB" scheme="http://zylyye.cn/tags/MSB/"/>
    
  </entry>
  
  <entry>
    <title>How to make your front end projects look better</title>
    <link href="http://zylyye.cn/2020/04/08/read-note/how-to-meke-your-front-end-look-better/"/>
    <id>http://zylyye.cn/2020/04/08/read-note/how-to-meke-your-front-end-look-better/</id>
    <published>2020-04-08T01:09:47.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="negative-space"><a href="#negative-space" class="headerlink" title="negative space"></a>negative space</h2><h2 id="font-paire"><a href="#font-paire" class="headerlink" title="font paire"></a>font paire</h2><ul><li><a href="https://fontpair.co/">https://fontpair.co/</a></li></ul><a id="more"></a><h2 id="color-scheme"><a href="#color-scheme" class="headerlink" title="color scheme"></a>color scheme</h2><ul><li><a href="http://colormind.io/">http://colormind.io/</a></li></ul><h2 id="structure-and-layout"><a href="#structure-and-layout" class="headerlink" title="structure and layout"></a>structure and layout</h2><h2 id="icons-and-images"><a href="#icons-and-images" class="headerlink" title="icons and images"></a>icons and images</h2><h2 id="css-animations"><a href="#css-animations" class="headerlink" title="css animations"></a>css animations</h2><ul><li><a href="https://daneden.github.io/animate.css/">https://daneden.github.io/animate.css/</a></li></ul><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><hr><ul><li><a href="https://www.freecodecamp.org/news/how-to-make-your-front-end-projects/">https://www.freecodecamp.org/news/how-to-make-your-front-end-projects/</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;negative-space&quot;&gt;&lt;a href=&quot;#negative-space&quot; class=&quot;headerlink&quot; title=&quot;negative space&quot;&gt;&lt;/a&gt;negative space&lt;/h2&gt;&lt;h2 id=&quot;font-paire&quot;&gt;&lt;a href=&quot;#font-paire&quot; class=&quot;headerlink&quot; title=&quot;font paire&quot;&gt;&lt;/a&gt;font paire&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;https://fontpair.co/&quot;&gt;https://fontpair.co/&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="阅读笔记" scheme="http://zylyye.cn/categories/%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/"/>
    
    
  </entry>
  
  <entry>
    <title>如何阅读一本书</title>
    <link href="http://zylyye.cn/2020/04/07/read-note/how-to-read-a-book/"/>
    <id>http://zylyye.cn/2020/04/07/read-note/how-to-read-a-book/</id>
    <published>2020-04-07T01:11:19.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="我是出于何种目的阅读"><a href="#我是出于何种目的阅读" class="headerlink" title="我是出于何种目的阅读"></a>我是出于何种目的阅读</h2><ul><li>为获取资讯（碎片化阅读，例如浏览新闻，朋友圈动态等）</li><li>为增进对某个领域的理解</li></ul><a id="more"></a><h2 id="我该怎么做"><a href="#我该怎么做" class="headerlink" title="我该怎么做"></a>我该怎么做</h2><h3 id="基础阅读"><a href="#基础阅读" class="headerlink" title="基础阅读"></a>基础阅读</h3><ul><li>理解语句的意思</li></ul><h3 id="检视阅读"><a href="#检视阅读" class="headerlink" title="检视阅读"></a>检视阅读</h3><ul><li>概览书的内容：序言、目录、感兴趣的章节及段落</li><li>粗读整书的内容，暂时略过不懂的地方</li></ul><h3 id="分析阅读"><a href="#分析阅读" class="headerlink" title="分析阅读"></a>分析阅读</h3><ul><li><p>带着基本问题阅读</p><ul><li>这本书整体上说了些什么</li><li>整本书在细部说了什么</li><li>这本书说的对吗</li><li>这本书与我们有何关联</li></ul></li><li><p>输出理解的内容</p><ul><li>结构笔记</li><li>概念笔记（书中的观点及自己的理解）</li><li>辩证笔记</li></ul></li><li><p>规则</p><ul><li>将书籍分类（明白书籍要讲述哪个领域的知识）</li><li>概括书籍（大体了解书籍要讲述哪些内容）</li><li>了解内容的整体结构</li><li>找出作者在问或想要回答的问题</li><li>保持同频道（对于使用到的关键字、专业术语及主题，确保自己正在理解的与作者所讨论的含义一致）</li><li>找出作者的观点及陈述的理由</li><li>找出作者的解答</li><li>做一个有修养的读者，以尊重的态度耐心听作者叙述完</li><li>做出理性的评价，反驳前准备好充分的理论依据</li></ul></li></ul><h3 id="主题阅读（针对不同书本的相同主题）"><a href="#主题阅读（针对不同书本的相同主题）" class="headerlink" title="主题阅读（针对不同书本的相同主题）"></a>主题阅读（针对不同书本的相同主题）</h3><ul><li>根据主题挑选内容</li><li>寻找作者们的共识</li><li>提出问题，寻求作者的解答</li><li>细化问题，寻求不同的观点解答</li><li>对不同的观点进行分析、总结</li></ul><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><hr><ul><li>[美] 莫提默·J. 艾德勒 / 查尔斯·范多伦《如何阅读一本书》</li><li><a href="https://zhuanlan.zhihu.com/p/23241617">https://zhuanlan.zhihu.com/p/23241617</a></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;我是出于何种目的阅读&quot;&gt;&lt;a href=&quot;#我是出于何种目的阅读&quot; class=&quot;headerlink&quot; title=&quot;我是出于何种目的阅读&quot;&gt;&lt;/a&gt;我是出于何种目的阅读&lt;/h2&gt;&lt;ul&gt;
&lt;li&gt;为获取资讯（碎片化阅读，例如浏览新闻，朋友圈动态等）&lt;/li&gt;
&lt;li&gt;为增进对某个领域的理解&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="阅读笔记" scheme="http://zylyye.cn/categories/%E9%98%85%E8%AF%BB%E7%AC%94%E8%AE%B0/"/>
    
    
      <category term="阅读" scheme="http://zylyye.cn/tags/%E9%98%85%E8%AF%BB/"/>
    
  </entry>
  
  <entry>
    <title>JAR包</title>
    <link href="http://zylyye.cn/2020/03/15/java-base/java-jar/"/>
    <id>http://zylyye.cn/2020/03/15/java-base/java-jar/</id>
    <published>2020-03-15T04:02:44.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>JAR 包是一个 ZIP 格式的压缩包, 通常使用 JAR 包来打包 Java 程序及其使用的相关资源文件, 以便于程序的发布、传输和运行。</p><a id="more"></a><h2 id="JAR-包的特点"><a href="#JAR-包的特点" class="headerlink" title="JAR 包的特点"></a>JAR 包的特点</h2><hr><ul><li>便于传输</li><li>压缩文件便于存储， 节省空间</li><li>提升安全性（可进行签名与验证）</li><li>可进行包密封（Package Sealing）</li><li>可用于扩展系统核心库</li><li>可携带额外的描述信息（发布者、版本等信息）</li><li>不依赖平台（portability）</li></ul><h2 id="使用-JAR-包"><a href="#使用-JAR-包" class="headerlink" title="使用 JAR 包"></a>使用 JAR 包</h2><hr><h3 id="创建-JAR-包"><a href="#创建-JAR-包" class="headerlink" title="创建 JAR 包"></a>创建 JAR 包</h3><p>使用 JDK 提供的打包命令行工具 jar 创建一个 JAR 包：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar cf file-name.jar files-or-directories</span><br></pre></td></tr></table></figure><p>例如，将当前文件夹下的 class 文件和 images 目录下的文件打包进 JAR 包中：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar cf app.jar *.class images</span><br></pre></td></tr></table></figure><p>该命令的其它可选选项：</p><table><thead><tr><th>选项</th><th>描述</th></tr></thead><tbody><tr><td>v</td><td>打包时显示详细信息，会显示所有打包文件的文件名</td></tr><tr><td>0(数字0)</td><td>指定压缩等级，0 表示不进行压缩</td></tr><tr><td>M</td><td>不自动生成 JAR 包的清单文件（manifest）</td></tr><tr><td>m</td><td>手动指定清单文件（manifest），打包时将会把指定清单文件中的内容放入 JAR 包</td></tr><tr><td>-C</td><td>临时更改 jar 命令执行时的当前文件夹</td></tr></tbody></table><p>对于 <code>-C</code> 命令，例如现在需要打包 images 目录下的所有图片，而不是连同 images 目录打包，可以使用 -C 来临时将当前目录更改为 images 目录进行打包：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar cf app.jar -C images .</span><br></pre></td></tr></table></figure><h3 id="查看-JAR-包"><a href="#查看-JAR-包" class="headerlink" title="查看 JAR 包"></a>查看 JAR 包</h3><p>查看一个 JAR 包内的文件列表：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar tf app.jar</span><br></pre></td></tr></table></figure><p>可以使用 v 选项来展示更详细的信息（文件字节大小、创建时间等）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar tvf app.jar</span><br></pre></td></tr></table></figure><h3 id="解压-JAR-包"><a href="#解压-JAR-包" class="headerlink" title="解压 JAR 包"></a>解压 JAR 包</h3><p>解压 JAR 包内的全部文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar xf app.jar</span><br></pre></td></tr></table></figure><p>解压 JAR 包内指定文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar xf app.jar filenames</span><br></pre></td></tr></table></figure><h3 id="更新-JAR-包"><a href="#更新-JAR-包" class="headerlink" title="更新 JAR 包"></a>更新 JAR 包</h3><p>更新 JAR 包的命令可以用来新增或覆盖 JAR 包中的文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar uf app.jar add-files</span><br></pre></td></tr></table></figure><p>如果 JAR 包中已包含指定的文件，则直接覆盖，否则新增</p><h3 id="运行-JAR-包"><a href="#运行-JAR-包" class="headerlink" title="运行 JAR 包"></a>运行 JAR 包</h3><p>一般的 JAR 包仅仅只是打包了需要的文件，如果希望 JAR 包可以直接运行，需要在 JAR 包的清单文件(manifest)中指定运行的主类(Main-CLass), 运行的方式如下：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -jar app.jar</span><br></pre></td></tr></table></figure><p><strong>还可以使用 javaw 来运行 JAR 包，区别是 Javaw 不会打开命令行窗口。</strong></p><p>还可以在运行 JAR 包时指定虚拟机参数，例如指定最大允许分配内存为 512 m：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -jar -Xmx512m app.jar</span><br></pre></td></tr></table></figure><h2 id="MANIFEST-文件"><a href="#MANIFEST-文件" class="headerlink" title="MANIFEST 文件"></a>MANIFEST 文件</h2><hr><p>MANIFEST 是 JAR 包的清单文件，用来存储 JAR 包和包中文件的属性信息，MANIFEST文件在 JAR 包中的存储路径为 META-INF/MANIFEST.MF。</p><h3 id="MANIFEST-文件的定义格式"><a href="#MANIFEST-文件的定义格式" class="headerlink" title="MANIFEST 文件的定义格式"></a>MANIFEST 文件的定义格式</h3><p><strong>属性命名方式：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Attr-Name: value</span><br></pre></td></tr></table></figure><p>属性名称中每个单词首字母大写，单词间使用中划线隔开，属性名称和属性值之间使用逗号隔开，例如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Manifest-Version: 1.0</span><br><span class="line">Main-Class: com.example.Main</span><br></pre></td></tr></table></figure><p><strong>清单文件中定义的属性分为两种</strong>：</p><ul><li>全局属性 - 针对整个 JAR 包及其中的所有文件</li><li>局部属性 - 针对某个文件夹或文件，局部属性会覆盖全局属性</li></ul><p>全局属性定义在清单文件的开始部分，局部属性定义在全局属性之后，例如：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Manifest-Version: 1.0</span><br><span class="line">Main-Class: com.example.Main</span><br><span class="line">Sealed: true</span><br><span class="line">Name: some/package/</span><br><span class="line">Sealed: false</span><br></pre></td></tr></table></figure><p>其中属性 Name 用于指定局部属性，该属性的值指定了作用的文件或文件夹（包）。</p><h3 id="创建、更新-MANIFEST-文件"><a href="#创建、更新-MANIFEST-文件" class="headerlink" title="创建、更新 MANIFEST 文件"></a>创建、更新 MANIFEST 文件</h3><p>MANIFEST 文件会在每次使用 <code>jar cf</code> 命令创建 JAR 包时自动生成（除非指定了 M 参数来禁用自动生成），默认生成的 MANEFIST 文件通常会包含一些基础的描述信息：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Manifest-Version: 1.0</span><br><span class="line">Created-By: 1.8.0_171 (Oracle Corporation)</span><br></pre></td></tr></table></figure><p>可以指定一个自定义的清单文件，来添加需要的属性：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar cfm app.jar manifest-add.mf files</span><br></pre></td></tr></table></figure><p>其中 manifest-add.mf 是纯文本文件，内容包含了自定义的属性内容，<strong>该操作会将自定义清单文件的内容添加到自动生成的清单文件中。</strong></p><h2 id="可运行的-JAR-包"><a href="#可运行的-JAR-包" class="headerlink" title="可运行的 JAR 包"></a>可运行的 JAR 包</h2><hr><p>如果希望 JAR 包可以执行，需要在 JAR 包中的清单文件中指定应用的入口：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Main-Class: com.example.classname</span><br></pre></td></tr></table></figure><p>Main-Class 来指定应用中包含 main 方法类的全限定名，有两种设置该属性的方式：</p><ul><li><p>在自定义清单文件中添加</p></li><li><p>使用 jar 命令打包时，使用选项 e 来指定 Main-Class 的值，例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jar cfe app.jar com.example.classname files</span><br></pre></td></tr></table></figure></li></ul><p>指定了 Main-Class 后的 JAR 包，可以使用 java 命令运行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -jar app.jar</span><br></pre></td></tr></table></figure><h2 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h2><hr><ul><li><p><a href="https://docs.oracle.com/javase/tutorial/deployment/jar/index.html">https://docs.oracle.com/javase/tutorial/deployment/jar/index.html</a></p></li><li><p><a href="https://docs.oracle.com/javase/10/docs/specs/jar/jar.html">https://docs.oracle.com/javase/10/docs/specs/jar/jar.html</a></p></li><li><p>《Java 核心技术卷》卷一 第 13 章 第十版</p></li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;JAR 包是一个 ZIP 格式的压缩包, 通常使用 JAR 包来打包 Java 程序及其使用的相关资源文件, 以便于程序的发布、传输和运行。&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
      <category term="JAR" scheme="http://zylyye.cn/tags/JAR/"/>
    
  </entry>
  
  <entry>
    <title>Java集合框架</title>
    <link href="http://zylyye.cn/2019/07/27/java-base/java-collections-framework/"/>
    <id>http://zylyye.cn/2019/07/27/java-base/java-collections-framework/</id>
    <published>2019-07-27T14:44:22.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>Java集合框架对常用的算法、数据结构进行了封装，组成了一套可以用于多种处理对象集合场景的框架.</p><a id="more"></a><h2 id="集合框架中的接口"><a href="#集合框架中的接口" class="headerlink" title="集合框架中的接口"></a>集合框架中的接口</h2><hr><p>Java 集合框架中为不同类型的集合定义了一个接口，并根据这个接口，提供了多种实现，几乎所有的集合接口或类都支持泛型，集合框架中定义了两大基础接口：Collection 与 Map</p><p>集合框架中所有的集合类都实现自这两个接口，其中 Collection 下包含了一以下几个核心接口：</p><ul><li>Set - 一个没有重复元素集<ul><li>SortedSet - 有序集</li></ul></li><li>List - 有序列表</li><li>Queue - 队列</li><li>Deque - 双端队列</li></ul><p>Map 中包含了以下核心接口：</p><ul><li>Map - 包含键值对的映射<ul><li>SortedMap - 按照键进行有序存储的映射</li></ul></li></ul><p>把功能抽象为接口与实现分离的设计使得这些集合类充分享有多态带来的灵活性，例如常见的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; strList = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br></pre></td></tr></table></figure><p>如果之后希望换成其他的数据结构的列表(例如链表结构的列表)，则可以直接替换，而无需担心影响已经使用了该对象的语句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; strList = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br></pre></td></tr></table></figure><p>官方文档给出了一个不同接口及实现的概览表格：</p><table><thead><tr><th align="left">Interface</th><th>Hash Table</th><th>Resizable Array</th><th>Balanced Tree</th><th>Linked List</th><th>Hash Table  +  Linked List</th></tr></thead><tbody><tr><td align="left"><code>Set</code></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/HashSet.html"><code>HashSet</code></a></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/TreeSet.html"><code>TreeSet</code></a></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/LinkedHashSet.html"><code>LinkedHashSet</code></a></td></tr><tr><td align="left"><code>List</code></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/ArrayList.html"><code>ArrayList</code></a></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/LinkedList.html"><code>LinkedList</code></a></td><td></td></tr><tr><td align="left"><code>Deque</code></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/ArrayDeque.html"><code>ArrayDeque</code></a></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/LinkedList.html"><code>LinkedList</code></a></td><td></td></tr><tr><td align="left"><code>Map</code></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/HashMap.html"><code>HashMap</code></a></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/TreeMap.html"><code>TreeMap</code></a></td><td></td><td><a href="https://docs.oracle.com/javase/10/docs/api/java/util/LinkedHashMap.html"><code>LinkedHashMap</code></a></td></tr></tbody></table><p>其中 Deque 是双端队列，是 Queue 的子接口.</p><h2 id="抽象集合类"><a href="#抽象集合类" class="headerlink" title="抽象集合类"></a>抽象集合类</h2><hr><p>集合框架中定义了几个实现基本接口的抽象类：</p><ul><li>AbstractCollection</li><li>AbstractSet</li><li>AbstractList</li><li>AbstractSequentialList</li><li>AbstractQueue</li><li>AbstractMap</li></ul><p>这些抽象类给出了接口的基本实现，方便自定义实现集合类.</p><h2 id="可选的方法与标记接口"><a href="#可选的方法与标记接口" class="headerlink" title="可选的方法与标记接口"></a>可选的方法与标记接口</h2><hr><h3 id="可选的方法"><a href="#可选的方法" class="headerlink" title="可选的方法"></a>可选的方法</h3><p>集合接口中定义的修改集合的方法不强制子类必须实现以提供支持(例如 add, remove, clear)，子类在实现集合接口时，可以根据需要在实现了这些接口的方法中抛出 UnsupportedOperationException 以表示当前的实现类不支持该操作.</p><p>例如 <code>Arrays.asList()</code> 方法返回的 List 是一个不可修改的列表，调用其 <code>add()</code> 方法时，会抛出 UnsupportedOperationException 异常.</p><h3 id="RandomAccess-标记接口"><a href="#RandomAccess-标记接口" class="headerlink" title="RandomAccess 标记接口"></a>RandomAccess 标记接口</h3><p>集合框架中使用 RandomAccess 接口来标记当前集合类支持高效的随机访问.</p><p>例如 ArrayList 类即有这个接口，因为 ArrayList 内部是使用数组来存储对象的，因此，可以支持使用存储索引来高效随机访问，但是 List 的另一种数据结构的实现 LinkedList 就不能高效的进行随机访问，因为其内部对象的存储基于双向链表结构，要想访问指定索引出的对象，必须对链表中的各个节点进行顺序遍历.</p><h2 id="Collection-接口"><a href="#Collection-接口" class="headerlink" title="Collection 接口"></a>Collection 接口</h2><hr><p>Collection 接口是所有集合的基础接口，其中定义了操作集合的基础方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回集合的大小</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">size</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断集合是否为空</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查询集合中是否包含指定元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">contains</span><span class="params">(Object o)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回一个集合元素迭代器</span></span><br><span class="line"><span class="function">Iterator&lt;E&gt; <span class="title">iterator</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 向集合中添加新元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">add</span><span class="params">(E e)</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 删除集合中指定的元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">remove</span><span class="params">(Object o)</span></span>;</span><br></pre></td></tr></table></figure><h3 id="遍历集合"><a href="#遍历集合" class="headerlink" title="遍历集合"></a>遍历集合</h3><p>有三种用来遍历集合中元素的方式：</p><ul><li>使用聚合操作（自 Java 8 起提供的 stream 流来操作集合）</li><li>使用 for-each 结构语句</li><li>使用迭代器</li></ul><h4 id="聚合操作-stream-流"><a href="#聚合操作-stream-流" class="headerlink" title="聚合操作(stream 流)"></a>聚合操作(stream 流)</h4><p>自 Java 8 起，可以通过 stream 流来对集合进行聚合操作，Collection 接口中定义了将集合转换为 stream 流的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回一个stream流</span></span><br><span class="line"><span class="function">Stream&lt;E&gt; <span class="title">stream</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回一个stream并行流</span></span><br><span class="line"><span class="function">Stream&lt;E&gt; <span class="title">parallelStream</span><span class="params">()</span></span></span><br></pre></td></tr></table></figure><p>例如对一个字符串集合进行筛选联结操作：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; strList = Arrays.asList(<span class="string">"aaa"</span>, <span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>);</span><br><span class="line">String result = strList.stream()</span><br><span class="line">        .filter(str -&gt; str.length() == <span class="number">1</span>)</span><br><span class="line">        .collect(Collectors.joining(<span class="string">","</span>));</span><br><span class="line">System.out.println(<span class="string">"result = "</span> + result);</span><br><span class="line"><span class="comment">//</span></span><br></pre></td></tr></table></figure><h4 id="使用-for-each-结构语句"><a href="#使用-for-each-结构语句" class="headerlink" title="使用 for-each 结构语句"></a>使用 for-each 结构语句</h4><p>Collection 实现了 Iterable 接口，对于实现了 Iterable 接口的类，可以使用 for-each 结构语句对其进行遍历：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 遍历列表中的字符串并打印</span></span><br><span class="line"><span class="keyword">for</span> (String content : list) &#123;</span><br><span class="line">    System.out.println(content);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Iterable 中还定义了 <code>forEach()</code> 方法用来对集合进行遍历，不过该方法的内部同样是使用 for-each 结构语句实现.</p><h4 id="使用迭代器"><a href="#使用迭代器" class="headerlink" title="使用迭代器"></a>使用迭代器</h4><p>调用集合中的 <code>iterator()</code> 会返回一个迭代器，迭代器接口 Iterator 中定义了如下的基础方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 判断是否还有待迭代的元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">hasNext</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回下一个元素，如果没有，则抛出NoSuchElementException</span></span><br><span class="line"><span class="function">E <span class="title">next</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 移除刚才迭代的元素，该方法在每次迭代后，只能调用一次</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">remove</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 处理剩余的元素</span></span><br><span class="line">forEachRemaining(Consumer&lt;? <span class="keyword">super</span> E&gt; action);</span><br></pre></td></tr></table></figure><p>迭代器典型的使用方式如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Iterator&lt;String&gt; iterator = list.iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    String nextStr = iterator.next();</span><br><span class="line">    System.out.println(nextStr);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用迭代器筛选字符串列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;(Arrays.asList(<span class="string">"aaa"</span>, <span class="string">"a"</span>, <span class="string">"b"</span>, <span class="string">"c"</span>));</span><br><span class="line">Iterator&lt;String&gt; iterator = list.iterator();</span><br><span class="line"><span class="keyword">while</span> (iterator.hasNext()) &#123;</span><br><span class="line">    String str = iterator.next();</span><br><span class="line">    <span class="keyword">if</span> (str.length() != <span class="number">1</span>) &#123;</span><br><span class="line">        iterator.remove();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">System.out.println(<span class="string">"list = "</span> + list);</span><br><span class="line"><span class="comment">// list = [a, b, c]</span></span><br></pre></td></tr></table></figure><p><strong>注意与使用 stream 流进行筛选的方式不同的是，使用迭代器删除会更改原始集合，使用 stream 流进行筛选不会对原集合产生副作用.</strong></p><p>默认情况下，迭代器只能向后迭代，不过 List 中可以使用 <code>listIterator()</code> 方法来获取一个列表迭代器，该迭代器支持向前迭代.</p><h3 id="批操作方法"><a href="#批操作方法" class="headerlink" title="批操作方法"></a>批操作方法</h3><p>Collection 接口中定义了一些有用的批操作方法接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 判断集合中是否包含指定集合中的全部元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">containsAll</span><span class="params">(Collection&lt;?&gt; c)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 将指定集合中的元素添加到本集合中</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">addAll</span><span class="params">(Collection&lt;? extends E&gt; c)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 移除包含在指定集合中的全部元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">removeAll</span><span class="params">(Collection&lt;?&gt; c)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 给定一个选择器，移除全部符合条件的元素</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">removeIf</span><span class="params">(Predicate&lt;? <span class="keyword">super</span> E&gt; filter)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 只保留和指定集合中相等的元素(取两集合交集)</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">retainAll</span><span class="params">(Collection&lt;?&gt; c)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 清除集合中的所有元素</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><p>例如移除集合中为 null 的元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">list.removeAll(Collections.singleton(<span class="keyword">null</span>));</span><br><span class="line"><span class="comment">// 或</span></span><br><span class="line">list.removeIf(Objects::isNull);</span><br></pre></td></tr></table></figure><h3 id="数组相关方法"><a href="#数组相关方法" class="headerlink" title="数组相关方法"></a>数组相关方法</h3><p>集合接口中定义了将集合转化为数组的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 转化为Object数组</span></span><br><span class="line">Object[] toArray();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 转化为指定类型数组，集合顺序填充数组a，剩余的填充null，如果a的长度不够，则创建一个新的数组填充返回</span></span><br><span class="line">&lt;T&gt; T[] toArray(T[] a);</span><br></pre></td></tr></table></figure><p>例如将字符串列表转化为字符串数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">String[] strings = list.toArray(<span class="keyword">new</span> String[<span class="number">0</span>]);</span><br></pre></td></tr></table></figure><h2 id="Set-接口"><a href="#Set-接口" class="headerlink" title="Set 接口"></a>Set 接口</h2><hr><p>Set 在是一种不包含重复元素的集合，Set 并没有在 Collection 的基础上增加新的方法，只是在新增时限制不能插入重复元素，利用该特性，可以为一个集合去重：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Set&lt;String&gt; uniList = <span class="keyword">new</span> HashSet&lt;&gt;(list);</span><br><span class="line"><span class="comment">// 或者使用stream流，一下语句返回一个HashSet实例</span></span><br><span class="line">Set&lt;String&gt; uniList = list.stream().collect(Collectors.toSet());</span><br></pre></td></tr></table></figure><p>此外 Set 中重写了 <code>equals()</code> 方法，当两个 Set 实例之间进行比较时，只要两者包含相同的元素(即使顺序不同)，则视为相等.</p><h3 id="基本实现类型"><a href="#基本实现类型" class="headerlink" title="基本实现类型"></a>基本实现类型</h3><p>Set 接口下主要包含了三种实现：</p><ul><li><p>HashSet - 最常用、性能最佳的实现，内部使用哈希表存储，元素无序</p></li><li><p>TreeSet - 使用红黑树结构存储，元素有序存储</p><p>TreeSet 始终会将元素维持为一个有序的树状结构(红黑树，一种自平衡二叉树)，可以实现高效的有序插入、删除操作.</p></li><li><p>LinkedHashSet - 使用链表结构存储，元素按照插入时的顺序存储</p><p>LinkedHashSet 内部使用链表结构的哈希表存储，可以保持元素插入的顺序(注意如果插入已存在的元素不会影响之前元素的存储顺序，因为 Set 只会插入集合中不存在的元素)，如果要对一个有序列表进行去重，则可以使用 LinkedHashSet，以保持有序列表中的位置：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Set&lt;String&gt; uniList = <span class="keyword">new</span> LinkedHashSet&lt;&gt;(sortedList);</span><br></pre></td></tr></table></figure></li></ul><p>除此之外, Set 接口下还包含两种特殊用途的实现:</p><ul><li>EnumSet - 高性能的枚举集</li><li>CopyOnWriteArraySet - 一个<strong>写时复制</strong>集, 所有的修改操作都会在内部复制一个新的数组, 避免影响修改前的数组. 在遍历该集合时, 可以无需担心并发修改的影响. 该实现适用于<strong>频繁遍历, 少数修改</strong>的场景, 例如一个不重复的事件处理器列表.</li></ul><h2 id="SortedSet-接口"><a href="#SortedSet-接口" class="headerlink" title="SortedSet 接口"></a>SortedSet 接口</h2><hr><p>SortedSet 是一个有序的集, 该集合中的元素以升序排序, 在创建 SortedSet 对象时, 可以指定一个比较器, 如果不指定比较器, 存储的元素必须实现 Comparable 接口来指定元素的排序规则. SortedSet 构造器可以接收一个集合, 如果这个集合也是 SortedSet 的一个实例, 则新建的有序集会沿用之前的比较器, 并将旧集合的元素依照原顺序插入到新集合中.</p><p>SortedSet 与普通 Set 最显著的区别是其将元素有序存储, 且该集返回的迭代器和 <code>toArray()</code> 返回的数组都可以用来有序遍历. 除此之外, SortedSet 还增加了几个额外的方法:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回有序集的第一个元素</span></span><br><span class="line"><span class="function">E <span class="title">first</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回有序集的最后一个元素</span></span><br><span class="line"><span class="function">E <span class="title">last</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回集合的一个范围视图 包含fromElement, 不包含toElement</span></span><br><span class="line"><span class="function">SortedSet&lt;E&gt; <span class="title">subSet</span><span class="params">(E fromElement, E toElement)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回集合开始部分的元素视图, 不包含toElement</span></span><br><span class="line"><span class="function">SortedSet&lt;E&gt; <span class="title">headSet</span><span class="params">(E toElement)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回集合结尾部分的元素视图, 包含fromElement</span></span><br><span class="line"><span class="function">SortedSet&lt;E&gt; <span class="title">tailSet</span><span class="params">(E fromElement)</span></span>;</span><br></pre></td></tr></table></figure><p>比较特别的是, 该接口中定义的几个返回视图的方法, 其中接收的参数(fromElement, toElement)是用来确定位置的参照元素, 这些元素并不一定要在集合中, 例如一个数字集的子视图:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">intSortedSet.subSet(<span class="number">3</span>, <span class="number">10</span>);</span><br></pre></td></tr></table></figure><p>返回的子视图包含元素x: <code>x &gt;= 3 &amp;&amp; x &lt; 10</code>, 由此, 文档中给出几个有趣的例子:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 统计以 a 开头的单词的数量, 因为按照字符串的排序规则, "ab" &gt; "a"</span></span><br><span class="line"><span class="keyword">int</span> count = wordSet.subset(<span class="string">"a"</span>, <span class="string">"b"</span>).size();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 统计以 a 开头的单词的数量, 但不包含 "a", 其中"\0"表示空字符, "a" &lt; "a\0" &lt; "ab"</span></span><br><span class="line"><span class="keyword">int</span> count = wordSet.subset(<span class="string">"a\0"</span>, <span class="string">"b"</span>).size();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 清除 a 到 b 之间的单词</span></span><br><span class="line">wordSet.subset(<span class="string">"a"</span>, <span class="string">"b"</span>).clear();</span><br></pre></td></tr></table></figure><p>同样的, 这些特性也能应用在<code>headSet()</code>, <code>tailSet()</code>上.</p><h2 id="List-接口"><a href="#List-接口" class="headerlink" title="List 接口"></a>List 接口</h2><hr><p>List 列表是一种有序集合，与 Set 不同，列表中可以包含重复元素. 此外，除了实现了集合中的基本接口外,列表中还额外添加了一些有用的功能方法:</p><ul><li><p>位置访问</p><p>列表中可以直接使用索引来访问元素,列表接口中定义了以下方法:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获取指定索引处的元素</span></span><br><span class="line"><span class="function">E <span class="title">get</span><span class="params">(<span class="keyword">int</span> index)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 设置指定索引除的元素,并返回之前的元素</span></span><br><span class="line"><span class="function">E <span class="title">set</span><span class="params">(<span class="keyword">int</span> index, E element)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 移除并返回指定索引处的元素</span></span><br><span class="line"><span class="function">E <span class="title">remove</span><span class="params">(<span class="keyword">int</span> index)</span></span>;</span><br></pre></td></tr></table></figure><p>List有基于数组和基于链表两种数据结构的实现,基于数组的实现可以高效的进行位置访问操作, 如 ArrayList, 而基于链表的实现, 访问指定索引出的元素时, 需要进行顺序遍历, 因此效率较低, 例如 LinkedList</p></li><li><p>位置搜索</p><p>列表中可以根据给定元素, 确定其在列表中的位置:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回指定元素第一次和最后一次出现的位置,没有则返回-1</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">indexOf</span><span class="params">(Object o)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">lastIndexOf</span><span class="params">(Object o)</span></span>;</span><br></pre></td></tr></table></figure></li><li><p>列表迭代器</p><p>与集合中默认的迭代器不同, 列表中的迭代器可以向前遍历, 列表中可以用以下方法获取一个列表迭代器:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回一个列表迭代器, 可以指定起始位置</span></span><br><span class="line"><span class="function">ListIterator&lt;E&gt; <span class="title">listIterator</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">ListIterator&lt;E&gt; <span class="title">listIterator</span><span class="params">(<span class="keyword">int</span> index)</span></span>;</span><br></pre></td></tr></table></figure></li><li><p>范围视图</p><p>列表可以返回一个范围内的列表视图, 列表返回的视图支持列表的所有操作:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回指定范围的子列表</span></span><br><span class="line"><span class="function">List&lt;E&gt; <span class="title">subList</span><span class="params">(<span class="keyword">int</span> fromIndex, <span class="keyword">int</span> toIndex)</span></span>;</span><br></pre></td></tr></table></figure><p>对 <code>sublist()</code> 方法返回的列表视图进行修改会直接影响到源列表, 由此,可以结合列表的其他方法来简化工作, 例如删除列表中的某个范围内的元素:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">list.subList(<span class="number">1</span>, <span class="number">5</span>).clear();</span><br></pre></td></tr></table></figure></li></ul><h3 id="基本实现类型-1"><a href="#基本实现类型-1" class="headerlink" title="基本实现类型"></a>基本实现类型</h3><p>合框架中的 List 有两种基本实现:</p><ul><li>ArrayList - 最常用的实现, 内部基于数组实现, 可以提供高效的随机访问与遍历</li><li>LinkedLisk - 内部基于链表结构, 可以提供高效的删除/插入操作</li></ul><p>初次之外, 列表的也有一个特殊用途的实现:</p><ul><li>CopyOnWriteArrayList - 一个<strong>写时复制</strong>列表, 与 CopyOnWriteArraySet 类似, 在遍历该集合时, 无需担心并发修改该集合的影响, 该实现同样适用于<strong>频繁遍历, 少数修改的情况</strong>.</li></ul><h2 id="Queue-接口"><a href="#Queue-接口" class="headerlink" title="Queue 接口"></a>Queue 接口</h2><hr><p>Queue 是一个队列, 用于保存待处理的元素集合, 队列接口在集合的基础上增加了额外的增删/获取方法, 方法作用及特点如下(表中只有 <code>add()</code> 不是新方法):</p><table><thead><tr><th align="center">操作类型</th><th align="center">失败时方法抛出抛出异常</th><th align="center">失败时返回特殊值 null/false</th></tr></thead><tbody><tr><td align="center">插入队列</td><td align="center">add(e)</td><td align="center">offer(e)</td></tr><tr><td align="center">获取并从队列移除</td><td align="center">remove()</td><td align="center">poll()</td></tr><tr><td align="center">获取但不从队列移除</td><td align="center">element()</td><td align="center">peek()</td></tr></tbody></table><p>当队列为空时, 调用获取元素的方法会返回 null, 因此, 一般队列的队列的实现中限制了不能存放 null (<code>LinkedList</code> 没有这个限制, 它允许插入 null), </p><h3 id="队列元素的顺序"><a href="#队列元素的顺序" class="headerlink" title="队列元素的顺序"></a>队列元素的顺序</h3><p>  Queue 的一般实现都是一个 FIFO(first in firt out, 先入先出)对列, 队列新增的元素会放置到队列的末尾, 需要取出元素时, 会从返回队列头部的元素. 不过也有可以指定排序规则的队列, 例如 PriorityQueue, 创建该队列时, 可以指定一个比较器(Compartor), 这样队列中的元素将按照指定的顺序排序. </p><h3 id="有界队列"><a href="#有界队列" class="headerlink" title="有界队列"></a>有界队列</h3><p>在 java.util 包中的队列实现都是无界的, java.util.concurrent 包中的大部分实现是有界的, 该包中的存放的都是线程安全的实现.</p><h3 id="Queue-基本实现类型"><a href="#Queue-基本实现类型" class="headerlink" title="Queue 基本实现类型"></a>Queue 基本实现类型</h3><p>队列的实现类型分为两种: <strong>一般实现类型</strong> 和 <strong>线程安全实现类型</strong>.</p><p><strong>一般实现类型</strong></p><p>常用的实现类如:</p><ul><li>LinkedList  - 一个FIFO(先入先出)的无界队列, 基于链表实现</li><li>PriorityQueue - 一个优先级队列, 队列的排序顺序可通过构造器指定, 没有则使用自然排序</li></ul><p><strong>线程安全实现类型</strong></p><p>线程安全的实现类都在包 java.util.concurrent 包下, 主要有:</p><ul><li>LinkedBlockingQueue - 有界 FIFO 阻塞队列, 基于链表实现</li><li>ArrayBlockingQueue - 有界 FIFO 阻塞队列, 基于数组实现</li><li>PriorityBlockingQueue - 无界优先队列</li><li>DelayQueue - 元素带有过期时间(存放的元素需实现 Delayed 接口), 有序队列</li><li>SynchronousQueue - 一个简单中转同步队列, 不存储元素</li><li>LinkedTransferQueue - 一个无界传输队列, 生产者调用其中的 <code>trasfer(E e)</code> 会阻塞, 直到有消费者进行消费</li></ul><h2 id="Deque-接口"><a href="#Deque-接口" class="headerlink" title="Deque 接口"></a>Deque 接口</h2><p>Deque 是一个双端队列, 与 Queue 队列不同的是, 双端队列可以在队列的两端进行元素的插入和删除操作, 其提供的操作队列的功能与 Queue 一致, 不过分为操作队首和队尾两个版本:</p><table><thead><tr><th align="center">操作</th><th>失败抛出异常</th><th>失败返回false/null</th></tr></thead><tbody><tr><td align="center">插入队首或队尾</td><td>addFirst(e)<br />addList(e)</td><td>offerFirst(e)<br />offerLast(e)</td></tr><tr><td align="center">获取并从队首或队尾删除</td><td>removeFirst(e)<br />removeLast(e)</td><td>pollFirst(e)<br />pollLast(e)</td></tr><tr><td align="center">获取队首或队尾的元素但不删除</td><td>getFirst()<br />getLast()</td><td>peekFirst()<br />peekLast()</td></tr></tbody></table><p>除了这些基本的操作队列方法外, Deque 中还提供了其它有关从队列中移除元素的方法:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 从队列中移除第一个或最后一个出现的指定元素, 如果移除成功则返回 true, 否则返回 false</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">removeFirstOccurrence</span><span class="params">(Object o)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">removeLastOccurrence</span><span class="params">(Object o)</span></span>;</span><br></pre></td></tr></table></figure><h3 id="Deque-基本实现类型"><a href="#Deque-基本实现类型" class="headerlink" title="Deque 基本实现类型"></a>Deque 基本实现类型</h3><p>双端队列的实现类同样分为两类: <strong>一般实现类型</strong>和<strong>线程安全的实现类型</strong>.</p><p><strong>一般实现类型</strong></p><ul><li>LinkedList - 基于链表结构的双端队列</li><li>ArrayDeque - 基于数组实现的双端队列, 类似于 ArrayList, 可自动扩容</li></ul><p><strong>线程安全的实现</strong></p><ul><li>ConcurrentLinkedDeque - 基于链表结构, 无界线程安全队列</li><li>LinkedBlockingDeque - 可通过构造器指定容量, 基于链表结构, 阻塞队列</li></ul><h2 id="Map-接口"><a href="#Map-接口" class="headerlink" title="Map`接口"></a>Map`接口</h2><hr><p>Map 不是属于集合, 它是一个存放键值对的映射表, 映射表中的键不会重复, 因此同一个键值对, 映射表中最多只会存在一个, 此外, 与队列不同, Map 中的键值都可为 null, Map 接口中定义了有关映射表的基础方法:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 存放指定键值对</span></span><br><span class="line"><span class="function">V <span class="title">put</span><span class="params">(K key, V value)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用键获取指定的值, 没有则返回 null</span></span><br><span class="line"><span class="function">V <span class="title">get</span><span class="params">(Object key)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取映射表中指定键对应的值, 没有则使用指定的默认值</span></span><br><span class="line"><span class="function">V <span class="title">getOrDefault</span><span class="params">(Object key, V defaultValue)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="comment">// 删除指定的键值</span></span></span><br><span class="line"><span class="function">V <span class="title">remove</span><span class="params">(Object key)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 获取映射表中键值对的数量</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">size</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断表映射是否为空</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断映射表中是否包含指定的键</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">containsKey</span><span class="params">(Object key)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断映射表中是否包含指定的值</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">containsValue</span><span class="params">(Object value)</span></span>;</span><br></pre></td></tr></table></figure><p>与其他集合类似, Map 中 <code>equals()</code> 方法的实现可以保证两个包含相等键值对的 Map 对象比较结果为 true, 而无需担心 Map 对象具体的实现类型</p><h3 id="Map-的基本实现类型"><a href="#Map-的基本实现类型" class="headerlink" title="Map 的基本实现类型"></a>Map 的基本实现类型</h3><p>Map接口的基本实现类型及特点:</p><ul><li><p>HashMap - 最常用, 最高效的 hash 表实现, 键值无序存放</p></li><li><p>TreeMap - 键有序存放, 内部是红黑树结构</p></li><li><p>LinkedHashMap - 键值使用双向链表存放, 可以保持键的插入顺序</p><p>LinkedHashMap 中元素的顺序默认使用元素第一次插入的顺序, 不过, 该实现类还提供了一种构造方法用于指定以访问顺序排序:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">LinkedHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity,</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">float</span> loadFactor,</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">boolean</span> accessOrder)</span></span>;</span><br></pre></td></tr></table></figure><p>其参数的含义如下:</p><ul><li>initialCapacity - Map 中的初始桶数, 每个桶会存放多个元素, 默认值为 16</li><li>loadFactor - 填充因子, 用于表示桶中存放元素个数的最大占比, 当占比达到此值时, 会增加新的桶, 重新散列存储元素到各个桶中, 默认为 0.75</li><li>accessOrder - 元素顺序, 默认为 false, 即为元素插入顺序, 如果设置为 true, 则为元素访问顺序</li></ul><p>同时, 该实现类中有一个 removeEldestEntry 方法, 该方法会在每次执行插入操作后调用, 如果该方法返回 true 则会移除最旧的元素, 利用该特性可以将该 Map 作为一个 LRU (Least Recently Used, 最近最少使用) 缓存, 例如实现一个最大容量 100 的 LRU 缓存:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LRUCache</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">LinkedHashMap</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">removeEldestEntry</span><span class="params">(Map.Entry&lt;K, V&gt; eldest)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> size() &gt; <span class="number">100</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><p>此外, Map 接口下还有三种特殊用途的实现:</p><ul><li>EnumMap - 以枚举作为键的映射表, 其内部使用数组实现, 拥有同数组相近的性能</li><li>WeakHashMap - 弱引用映射表, 其性能与 HashMap 类似, 该实现的特别之处在于, 当值对应的键不在被外部引用, 则该键值对会在垃圾回收时被回收释放</li><li>IdentityHashMap - 该映射内部的比较不是使用 <code>equals()</code> 方法, 而是使用 == , 因此需要只有两个对象内存地址相同, 才会被视为相等</li></ul><p>线程安全的实现:</p><ul><li>ConcurrentHashMap  高效的线程安全 Map, 特性与 HashMap 类似, 键值无序</li><li>ConcurrentSkipListMap  有序的线程安全 Map, 元素默认按照<strong>键</strong>的自然顺序排序, 也可指定一个自定义比较器</li></ul><h3 id="批操作接口"><a href="#批操作接口" class="headerlink" title="批操作接口"></a>批操作接口</h3><p>Map 中定义了与集合类似的批操作接口:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 复制指定映射表中的所有键值对到当前映射表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">putAll</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 清空映射表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><p><code>putAll()</code> 方法通常用来合并一个映射表, 如果只是想简单的复制一个映射表, 直接使用构造方法即可:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String&gt; mapCopies = <span class="keyword">new</span> HashMap(map);</span><br></pre></td></tr></table></figure><h3 id="集合视图"><a href="#集合视图" class="headerlink" title="集合视图"></a>集合视图</h3><p>Map 接口中定义了三个与集合视图相关的方法, 分别返回三种视图:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 返回键的集</span></span><br><span class="line"><span class="function">Set&lt;K&gt; <span class="title">keySet</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回值的集合</span></span><br><span class="line"><span class="function">Collection&lt;V&gt; <span class="title">values</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回键值对的集</span></span><br><span class="line">Set&lt;Map.Entry&lt;K, V&gt;&gt; entrySet();</span><br></pre></td></tr></table></figure><h4 id="集合视图的巧妙用处"><a href="#集合视图的巧妙用处" class="headerlink" title="集合视图的巧妙用处"></a>集合视图的巧妙用处</h4><p>Map 对象返回的集合视图是可以修改的, 这些修改会直接影响到原有的映射表, 例如:</p><ul><li><p>删除指定的键:</p>  <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">map.keySet().removeAll(keyList);</span><br></pre></td></tr></table></figure></li><li><p>保留两个映射表共同键的部分:</p>  <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">map1.keySet().retainAll(map2.keySet());</span><br></pre></td></tr></table></figure></li><li><p>删除表中指定的值:</p>  <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">map.values().removeAll(Collections.singleton(specValue));</span><br></pre></td></tr></table></figure></li><li><p>删除表中键和值相等的数据:</p>  <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">map1.keySet().removeAll(map1.values());</span><br></pre></td></tr></table></figure></li></ul><p>此外, 可以使用集合视图来遍历映射表:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (Map.Entry&lt;String, Integer&gt; entry : map.entrySet()) &#123;</span><br><span class="line">    String key = entry.getKey();</span><br><span class="line">    Integer value = entry.getValue();</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>需留意, 调用 Map 对象获取视图的方法时, Map 对象中会缓存创建的视图, 如果多次调用, 返回的将是同一个视图对象, 即:</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">map.keySet() == map.keySet(); <span class="comment">// true</span></span><br><span class="line">map.values() == map.values();  <span class="comment">// true</span></span><br><span class="line">map.entrySet() == map.entrySet();  <span class="comment">// true</span></span><br></pre></td></tr></table></figure><h3 id="Stream-流的聚合操作"><a href="#Stream-流的聚合操作" class="headerlink" title="Stream 流的聚合操作"></a>Stream 流的聚合操作</h3><p>Stream 流只能用于处理集合, 不能直接处理 Map 对象, 使用 Stream 流提供的方法可以将一个集合转换为 Map 对象, 例如将 Student 对象集合转换为使用 id 作为键的映射表:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;Integer, Student&gt; studentMap = students.stream().collect(Collectors.toMap(Student::getId, Function.identity()));</span><br></pre></td></tr></table></figure><p>按照性别将 Student 对象集合分组:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;Integer, List&lt;Student&gt;&gt; genderMap = students.stream().collect(Collectors.groupingBy(Student::getGender));</span><br></pre></td></tr></table></figure><h2 id="SortedMap-接口"><a href="#SortedMap-接口" class="headerlink" title="SortedMap 接口"></a>SortedMap 接口</h2><hr><p>SortedMap 是有序的映射表, 该接口定义的功能与 SortedSet 类似 (SortedSet 内部就是使用 SortedMap 来存储元素),  SortedMap 对存储的键进行排序, 因此, 在创建 SortedMap 对象时, 可以为键指定一个比较器, 或者键需要实现 Comparable 接口.</p><p>SortedMap 很多特性与 SortedSet 相同:</p><ul><li>构造方法如果接收一个 SortedMap 对象, 则会保留该对象的比较规则和元素顺序</li><li>可以进行有序遍历</li><li>额外定义了获取端点 (EndPoints) 和范围视图的方法</li></ul><p>其在 Map 接口基础上额外定义的方法如下:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function">K <span class="title">firstKey</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">K <span class="title">lastKey</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">SortedMap&lt;K,V&gt; <span class="title">subMap</span><span class="params">(K fromKey, K toKey)</span></span>;</span><br><span class="line"><span class="function">SortedMap&lt;K,V&gt; <span class="title">headMap</span><span class="params">(K toKey)</span></span>;</span><br><span class="line"><span class="function">SortedMap&lt;K,V&gt; <span class="title">tailMap</span><span class="params">(K fromKey)</span></span>;</span><br><span class="line"><span class="function">SortedMap&lt;K,V&gt; <span class="title">subMap</span><span class="params">(K fromKey, K toKey)</span></span>;</span><br></pre></td></tr></table></figure><h2 id="参考文档"><a href="#参考文档" class="headerlink" title="参考文档"></a>参考文档</h2><hr><ol><li><a href="https://docs.oracle.com/javase/tutorial/collections/index.html">https://docs.oracle.com/javase/tutorial/collections/index.html</a></li><li><a href="https://docs.oracle.com/javase/10/docs/api/java/util/package-summary.html">API文档</a></li></ol>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Java集合框架对常用的算法、数据结构进行了封装，组成了一套可以用于多种处理对象集合场景的框架.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
      <category term="集合框架" scheme="http://zylyye.cn/tags/%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6/"/>
    
  </entry>
  
  <entry>
    <title>Java泛型</title>
    <link href="http://zylyye.cn/2018/07/20/java-base/java-generic-type/"/>
    <id>http://zylyye.cn/2018/07/20/java-base/java-generic-type/</id>
    <published>2018-07-20T13:29:26.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>Java泛型自Java 1.5 引入，可以通过为类或方法添加类型参数定义泛型类或方法，使用泛型可以提高类或方法的通用性，还可以借助编译器的类型检查来尽可能的避免类型的不安全转换.</p><a id="more"></a><h2 id="泛型类"><a href="#泛型类" class="headerlink" title="泛型类"></a>泛型类</h2><hr><p>定义一个类时，在类名后添加一个使用尖括号(&lt;&gt;)括起来的类型参数列表，这样的类即为泛型类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">GenericClassDemo</span>&lt;<span class="title">T</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T <span class="keyword">var</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getVar</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">var</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setVar</span><span class="params">(T t)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> = t;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样，在这个类中，就可以使用这些在定义在类型参数列表中的类型变量.</p><p>在实例化一个泛型类时，可以根据需要传入某个具体类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">GenericClassDemo&lt;String&gt; gcd = <span class="keyword">new</span> GenericClassDemo&lt;&gt;();</span><br></pre></td></tr></table></figure><p>注意：</p><ul><li>定义泛型类时，尖括号(&lt;&gt;)中的类型参数可以有多个，多个之间使用逗号隔开</li><li>泛型参数不能是基本类型(如int, double等)，相应的可以使用基本类型的包装类型取代</li><li>类型变量可以用于类中的域、方法参数、方法返回类型</li></ul><p><strong>Java中定义类型变量一般使用简短大写的字母，通常情况下，使用<code>E</code>表示集合元素的类型，<code>K</code>和<code>V</code>分别表示键(key)和值(value)的类型,<code>T</code>表示任意类型.</strong></p><h2 id="泛型方法"><a href="#泛型方法" class="headerlink" title="泛型方法"></a>泛型方法</h2><hr><p>与泛型类类似，为一个方法添加类型参数列表即可定义一个泛型方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; <span class="function">T <span class="title">getT</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">public</span> &lt;U&gt; <span class="function">U <span class="title">getU</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>泛型方法可以定义在一个普通类中，它与泛型类没有直接关联，调用一个泛型方法时，需要在方法名前提供类型参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">String str = Demo.&lt;String&gt;getT();</span><br><span class="line"><span class="keyword">int</span> i = <span class="keyword">new</span> Demo().&lt;Integer&gt;getU();</span><br></pre></td></tr></table></figure><p>注意：</p><ul><li>泛型方法的泛型参数列表定义在方法修饰符之后(如<code>public static</code>)，方法返回类型之前</li><li>泛型方法的泛型参数可以在方法的参数列表、方法返回类型、方法体中使用</li><li>调用泛型方法时，有时不需要提供类型参数，因为编译器可以根据语义自动推断出所需类型</li></ul><h2 id="泛型的类型变量限定"><a href="#泛型的类型变量限定" class="headerlink" title="泛型的类型变量限定"></a>泛型的类型变量限定</h2><hr><p>在定义泛型类或泛型方法时，可以为类型变量添加限定，以此来限定接收的类型，例如定义一个限定类型为<code>Comparable</code>或其子类的泛型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">Comparable</span>&gt; </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样在实例化这个类时，指定的类型参数只能为<code>Comparable</code>或其子类类型.</p><p><strong>由于Java中的类只能继承一个类，但可以实现多个方法，因此如果在限定的条件是某个类和者多个接口，可以使用(&amp;)符号隔开，如果有类作为限定，则该类必须作为限定列表中的第一个</strong>, 例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span>&lt;<span class="title">T</span> <span class="keyword">extends</span> <span class="title">ClassName</span> &amp; <span class="title">Interface1</span> &amp; <span class="title">Interface2</span>...&gt; </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="通配符类型"><a href="#通配符类型" class="headerlink" title="通配符类型"></a>通配符类型</h2><hr><p>如果一个方法接收一个具体泛型类的实例，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">(ArrayList&lt;String&gt; list)</span></span>&#123;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>那么这个方法只能接收<code>ArrayList&lt;String&gt;</code>对象，而不能接收指定其它泛型类型例如：<code>ArrayList&lt;Person&gt;</code>，当然，可以定义泛型方法来处理这种情况：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function"><span class="keyword">void</span> <span class="title">handle</span><span class="params">(ArrayList&lt;T&gt; list)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>除此之外，还可以使用通配符类型来处理这种情况，Java中可以使用符号<code>?</code>来表示任意一种类型，例如如下方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">handle</span><span class="params">(ArrayList&lt;?&gt; list)</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>则可以接收任意<code>ArrayList</code>的泛型对象.</p><h3 id="通配符上下限"><a href="#通配符上下限" class="headerlink" title="通配符上下限"></a>通配符上下限</h3><p>通配符上限使用关键字<code>extends</code>指定，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ArrayList&lt;? extends Serializable&gt;</span><br></pre></td></tr></table></figure><p><code>? extends Serializable</code>表示通配<code>Serializable</code>或其子类类型.</p><p>通配符下限使用关键字<code>super</code>指定，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ArrayList&lt;? <span class="keyword">super</span> Serializable&gt;</span><br></pre></td></tr></table></figure><p><code>? super Serializable</code>表示通配<code>Serializable</code>或其父类型.`</p><h2 id="泛型的类型擦除"><a href="#泛型的类型擦除" class="headerlink" title="泛型的类型擦除"></a>泛型的类型擦除</h2><hr><p>Java虚拟机并不实际支持泛型，Java的泛型特性主要是由编译器支持，代码中定义的泛型类，编译后由虚拟机解析运行时，仍然是一个普通的类.</p><p><strong>编译器在解析编译泛型类时，会在变量访问、方法调用等合适的地方插入类型转换，然后擦除泛型变量类型.</strong></p><p>例如对于一个泛型类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entry</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    K key;</span><br><span class="line">    V value;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Demo</span><span class="params">(K k, V v)</span> </span>&#123;</span><br><span class="line">        key = k;</span><br><span class="line">        value = v;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> K <span class="title">getKey</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> key;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> V <span class="title">getValue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>编译后，泛型变量会被擦除，只保留了泛型类的原始类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entry</span> </span>&#123;</span><br><span class="line">    Object key;</span><br><span class="line">    Object value;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Demo</span><span class="params">(Object k, Object v)</span> </span>&#123;</span><br><span class="line">        key = k;</span><br><span class="line">        value = v;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">getKey</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> key;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">getValue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> value;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果使用泛型类型限定，则会替换为限定的类，以尽量减少不必要的类型转换，如泛型类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entry</span>&lt;<span class="title">K</span> <span class="keyword">extends</span> <span class="title">Integer</span>, <span class="title">V</span> <span class="keyword">extends</span> <span class="title">String</span>&gt; </span>&#123;</span><br><span class="line">     ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>擦除后：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Entry</span> </span>&#123;</span><br><span class="line">    Integer key;</span><br><span class="line">    String value;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果限定列表中包含多个限定，则选中第一个类型.</p><h2 id="类型擦除带来的问题和约束"><a href="#类型擦除带来的问题和约束" class="headerlink" title="类型擦除带来的问题和约束"></a>类型擦除带来的问题和约束</h2><hr><ul><li><p><strong>泛型类型不能使用基本类型</strong>  类型擦除后，所有的泛型类型都变为<code>Object</code>，而基本类型不属于<code>Object</code>的子类，因此只能使用基本类型的包装类型来作为泛型类型(如<code>Integer</code>, <code>Double</code>等)</p></li><li><p><strong>不能使用<code>instanceof</code>来校验具体的泛型类</strong></p><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">obj <span class="keyword">instanceof</span> ArrayList&lt;String&gt;</span><br></pre></td></tr></table></figure><p>类型擦除后，不能保留具体的泛型类型</p></li><li><p><strong>不安全的泛型类数组</strong></p><p>例如创建一个泛型类数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Entry&lt;String, String&gt;[] entrys = <span class="keyword">new</span> Entry&lt;&gt;[<span class="number">10</span>];</span><br></pre></td></tr></table></figure><p><code>Entry&lt;String, String&gt;</code>类也是<code>Object</code>的子类，因此可以进行以下转换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Object[] objs = entrys;</span><br></pre></td></tr></table></figure><p>这样会导致如下的非法的赋值操作通过了编译器检查：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">objs[<span class="number">0</span>] = <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>这样的语句在运行时会发生<code>ArrayStoreException</code>异常，而编译时却不会发现这样的问题.</p><p>可以使用集合类来避免这个问题，例如<code>List&lt;Entry&lt;String, String&gt;&gt;</code>，泛型类型参数不存在多态的特性，因此可以避免上述的问题，如以下语句不会通过编译器检查：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Object&gt; = <span class="keyword">new</span> ArrayList&lt;Entry&lt;String, String&gt;&gt;;</span><br></pre></td></tr></table></figure></li><li><p><strong>不能直接使用泛型变量创建实例或访问其<code>Class</code>对象</strong></p><p>不能使用诸如<code>new T()</code>, <code>T.class</code>的语句，因为类型擦除后，<code>T</code>类型会被擦除为<code>Object</code></p></li></ul><p>泛型带来的问题根源于编译器的类型擦除以及Java中类的多态性，致使部分不合理的操作会通过编译器的检查成功编译，最后只会在运行期间发现错误.</p><h2 id="利用反射获取泛型类的信息"><a href="#利用反射获取泛型类的信息" class="headerlink" title="利用反射获取泛型类的信息"></a>利用反射获取泛型类的信息</h2><hr><p>虽然进行了类型擦除，依然可以通过反射来获取一点有关擦除前泛型类的信息，例如可以通过泛型类<code>Class</code>对象的<code>getTypeParameters()</code>来获取泛型类定义的泛型参数.</p><p><code>getTypeParameters()</code>方法返回了一个<code>TypeVariable&lt;Class&lt;T&gt;&gt;[]</code>数组，其中包含了定义的泛型参数列表的信息，例如对于集合类中的<code>ArrayList</code>类，可以通过该方法了解到该泛型类定义了一个泛型参数<code>E</code>.</p><h3 id="获取泛型类的具体类型"><a href="#获取泛型类的具体类型" class="headerlink" title="获取泛型类的具体类型"></a>获取泛型类的具体类型</h3><p>但是，通过上述方法无法获取具体的泛型类型，例如对一个<code>ArrayList&lt;String&gt;</code>类型的对象，无法通过反射获取到泛型变量的值为<code>String</code>，不过，子类会保留父类的泛型信息，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">CustomList</span> <span class="keyword">extends</span> <span class="title">ArrayList</span>&lt;<span class="title">String</span>&gt; </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此时，可以通过<code>CustomList</code>的<code>Class</code>对象获取到其超类的泛型信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">CustomList.class.getGenericSuperclass();</span><br></pre></td></tr></table></figure><p>获取具体的泛型类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Type[] types = ((ParameterizedTypeImpl) CustomList.class.getGenericSuperclass()).getActualTypeArguments();</span><br><span class="line">Type stringType = types[<span class="number">0</span>];</span><br></pre></td></tr></table></figure><p>这样即可以获取到实际的泛型类型是<code>String</code>.</p><p>因此，如需保留泛型类的具体信息，可以创建一个其匿名子类，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; list = <span class="keyword">new</span> ArrayList&lt;String&gt;() &#123;&#125;;</span><br></pre></td></tr></table></figure><p>这个<code>list</code>的<code>Class</code>对象中就保留了泛型的具体信息.</p><h2 id="类型擦除引发的序列化、反序列化问题"><a href="#类型擦除引发的序列化、反序列化问题" class="headerlink" title="类型擦除引发的序列化、反序列化问题"></a>类型擦除引发的序列化、反序列化问题</h2><hr><p>为了方便传输或保存，经常需要将对象进行序列化，例如接口将数据序列化为json，传递给前台使用. 在对泛型序列化时，一般不会存在什么问题，因为泛型类中的泛型实例有<code>Class</code>对象用于保存类型信息，但是如果对此进行反序列化，则会因为类型擦除导致不知道反序列化为哪个类型，例如(以google的Gson为例)：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">List&lt;UserInfo&gt; userList = ...;</span><br><span class="line"><span class="comment">// 序列化，仍然会保留数据的结构</span></span><br><span class="line">String json = gson.toJson(userList);</span><br><span class="line"><span class="comment">// 由于类型擦除，反序列化之后，集合中存储的是Map类型的对象，而不是UserInfo类型</span></span><br><span class="line">List&lt;UserInfo&gt; users = gson.fromJson(json);</span><br><span class="line"><span class="comment">// 运行期间抛出ClassCastException</span></span><br><span class="line">UserInfo user = users.get(<span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>为此，Gson使用了<code>TypeToken</code>来解决这个问题，其本质依然是因为泛型类的子类会记录父类的泛型具体信息：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 创建匿名内部类用于保存泛型信息</span></span><br><span class="line">Type userInfoType = <span class="keyword">new</span> TypeToken&lt;List&lt;UserInfo&gt;&gt;() &#123;&#125;.getType();</span><br><span class="line">List&lt;UserInfo&gt; users = gson.fromJson(json);</span><br><span class="line">UserInfo user = users.get(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Java泛型自Java 1.5 引入，可以通过为类或方法添加类型参数定义泛型类或方法，使用泛型可以提高类或方法的通用性，还可以借助编译器的类型检查来尽可能的避免类型的不安全转换.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
      <category term="Java泛型" scheme="http://zylyye.cn/tags/Java%E6%B3%9B%E5%9E%8B/"/>
    
  </entry>
  
  <entry>
    <title>Java日志API</title>
    <link href="http://zylyye.cn/2018/07/18/java-base/java-logger-api/"/>
    <id>http://zylyye.cn/2018/07/18/java-base/java-logger-api/</id>
    <published>2018-07-18T13:36:58.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>在编写程序时，经常需要使用输出工具（例如常用的<code>System.out.println()</code>）来输出、记录或者对比某些变量的值，在程序的运行期间，需要记录一些关键日志信息来调试、监控或者方便以后排查问题，此时，一般输出语句无法满足日志记录的需求，因此，Java内部提供了一套用于日志记录的API.</p><a id="more"></a><h2 id="日志API提供的好处"><a href="#日志API提供的好处" class="headerlink" title="日志API提供的好处"></a>日志API提供的好处</h2><hr><p>使用专门的日志工具来记录日志可以让这项工作更加专业和灵活性，例如：</p><ul><li>日志信息更加统一、标准</li><li>可以配置记录日志的级别（如只记录错误信息，忽略调试信息）</li><li>可以更容易的定位错误日志发生的位置</li><li>可以更方便的配置日志保存的位置</li></ul><h2 id="记录简单的日志"><a href="#记录简单的日志" class="headerlink" title="记录简单的日志"></a>记录简单的日志</h2><hr><p>需要记录一个日志只需要简单的几个步骤：</p><ol><li><p>获取日志记录器</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获取全局日志记录器</span></span><br><span class="line">Logger logger = Logger.getGlobal();</span><br></pre></td></tr></table></figure></li><li><p>打印指定级别的日志</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">logger.info(<span class="string">"global message"</span>);</span><br></pre></td></tr></table></figure></li></ol><p>这样，在程序中插入这条日志记录语句，控制台就会输出打印日志的具体类，具体方法以及自定义的日志信息.</p><h2 id="日志级别"><a href="#日志级别" class="headerlink" title="日志级别"></a>日志级别</h2><hr><p>日志API中定义了几个日志级别来帮助我们打印不同重要程度的日志：</p><ul><li>SEVERE</li><li>WARNING</li><li>INFO</li><li>CONFIG</li><li>FINE</li><li>FINER</li><li>FINEST</li></ul><p>这些级别中，最严重的是SEVERE级别，最普通的是FINEST级别，当程序发生严重错误时，应该记录SEVERE级别的日志，进行调试时或记录一般信息时，可以记录INFO级别或者更低级别(如FINE)的日志.</p><h3 id="配置日志记录级别"><a href="#配置日志记录级别" class="headerlink" title="配置日志记录级别"></a>配置日志记录级别</h3><p>获取日志记录器后，可以使用<code>Logger</code>对象提供的<code>setLevel()</code>方法设置记录的最大级别，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">logger.setLevel(Level.WARNING);</span><br></pre></td></tr></table></figure><p>这样级别在WARNING以下的日志将不会记录，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">logger.info(<span class="string">"info log"</span>); <span class="comment">// 不会打印</span></span><br></pre></td></tr></table></figure><h2 id="日志记录器的层次结构"><a href="#日志记录器的层次结构" class="headerlink" title="日志记录器的层次结构"></a>日志记录器的层次结构</h2><hr><p>获取日志记录器时，一般不使用<code>Logger.getGlobal()</code>来获取并使用全局日志记录器，通常情况下，应该为日志记录器提供一个名称，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Logger.getLogger(<span class="string">"com.example.tech"</span>);</span><br></pre></td></tr></table></figure><p>全局记录器也是一个普通的记录器，只是其名称为<code>global</code>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Logger.getGlobal() == Logger.getLogger(<span class="string">"global"</span>);<span class="comment">// true</span></span><br></pre></td></tr></table></figure><p>需要注意的是，这个像包名一样的名称还代表这一个层次结构，如名字为<code>com.example</code>的日志记录器就是<code>com.example.tech</code>的父级记录器，父级和子级之间存在着配置继承关系，例如在子级记录器中没有配置日志记录级别，则会默认使用父级设置的级别.</p><h3 id="获取父记录器及根日志记录器"><a href="#获取父记录器及根日志记录器" class="headerlink" title="获取父记录器及根日志记录器"></a>获取父记录器及根日志记录器</h3><p>可以使用<code>Logger</code>对象的<code>getParent()</code>来获取当前记录器的父记录器，日志记录器的层次结构类似于类的继承关系，所有类都有一个公共父类<code>Object</code>，类似的，所有日志记录器都有个公共父记录器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Logger.getLogger(<span class="string">""</span>);<span class="comment">// 根日志记录器</span></span><br></pre></td></tr></table></figure><p>所有的日志记录器都是其子级，包括全局记录器（因为全局记录器只是名字为<code>global</code>的普通记录器），根日志记录器有默认配置(如默认的日志记录级别为INFO)，每次获取的日志记录器将默认使用该默认配置.</p><p><strong>根日志记录器的默认配置文件位于JDK目录下的<code>jre/lib/logging.properties</code>，可以通过修改该文件中的配置来修改根日志记录器的默认配置.</strong></p><h2 id="日志处理器-Handler"><a href="#日志处理器-Handler" class="headerlink" title="日志处理器(Handler)"></a>日志处理器(Handler)</h2><hr><p>可以将日志记录器看成一个日志信息的收集器，记录器只是收集指定级别的日志，这些日志应该输出到什么地方则交由日志处理器来处理，<code>Logger</code>对象中定义了<code>addHandler()</code>方法用于向一个日志记录器添加处理器. 每个日志记录器可以添加多个处理器.</p><h3 id="预定义的日志处理器"><a href="#预定义的日志处理器" class="headerlink" title="预定义的日志处理器"></a>预定义的日志处理器</h3><ul><li><p>ConsoleHandler</p><p>该处理器会将日志输出到控制台中，默认的日志记录器就带有该处理器，因此，无需任何配置，记录日志时，控制台就会打印信息.</p></li><li><p>FileHandler</p><p>该处理器用于将日志输出到文件中.</p></li><li><p>SocketHandler</p><p>该处理器用于将日志输出到Socket连接中.</p></li><li><p>StreamHandler</p><p>该处理器用于将日志输出到指定输出流中，ConsoleHandler、FileHandler、SocketHandler就是继承自该处理器.</p></li><li><p>MemoryHandler</p><p>该处理器用于将日志临时缓存在内存中(最大缓存1000条，超出后，旧的日志将被新的覆盖)，在调用<code>push()</code>方法时，缓存的日志将被输出到其中配置好的处理器中，此外，该处理器有个主动输出级别阀值(PushLevel，可调用<code>setPushLevel()</code>配置该阀值)，当记录的日志级别等于或高于该阀值时，就会主动调用<code>push()</code>.</p></li></ul><p>如果有特殊需求，可以自己继承<code>java.util.logging.Handler</code>来自定义一个日志处理器.</p><h3 id="日志处理器的级别"><a href="#日志处理器的级别" class="headerlink" title="日志处理器的级别"></a>日志处理器的级别</h3><p>日志记录器只收集指定级别的日志，日志处理器也需要配置只处理指定级别的日志(默认为INFO)，它们之间互相独立，多个日志处理器之间也可以分别设置不同的处理级别. <code>Handler</code>对象提供了<code>setLevel()</code>方法用于设置处理器的处理级别.</p><h3 id="日志处理器的过滤器"><a href="#日志处理器的过滤器" class="headerlink" title="日志处理器的过滤器"></a>日志处理器的过滤器</h3><p>每个日志处理器还可以使用<code>setFilter()</code>设置一个过滤器(<code>Filter</code>)，如果需要更加细粒的过滤，可以实现<code>java.util.logging.Filter</code>接口来自定义日志的记录条件，该接口中只有一个抽象方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isLoggable</span><span class="params">(LogRecord record)</span></span>;</span><br></pre></td></tr></table></figure><p><code>LogRecord</code>对象中包含有日志的详细信息(例如日志级别、内容等等)，可以根据这些信息实现自己的日志过滤需求.</p><h3 id="日志处理器的格式化器"><a href="#日志处理器的格式化器" class="headerlink" title="日志处理器的格式化器"></a>日志处理器的格式化器</h3><p>如果需要自定义日志的格式，可以使用<code>setFormatter()</code>为日志处理器设置一个日志格式化器，Java内置了两种日志格式化器：</p><ul><li><p>SimpleFormatter</p><p>用于将日志格式化为简洁可读的格式，ConsoleHandler默认使用的就是该格式化器.</p></li><li><p>XMLFormatter</p><p>将日志格式化为XML文档格式，方便存储分析，FileHandler默认使用的为该格式化器.</p></li></ul><p>实现自定义的格式化器时，只需继承<code>java.util.logging.Formatter</code>对象，并实现以下一个方法即可：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> String <span class="title">format</span><span class="params">(LogRecord record)</span></span>;</span><br></pre></td></tr></table></figure><h2 id="日志管理器"><a href="#日志管理器" class="headerlink" title="日志管理器"></a>日志管理器</h2><hr><p>Java中有一个全局唯一的日志管理器LogManager，日志管理器中维护了日志记录器和相关功能的状态，其功能主要包括：</p><ul><li>管理不同日志管理器的层次结构</li><li>管理日志的配置属性</li></ul><p>全局的日志管理器方便了日志记录器的获取、存储和管理，在整个程序中，通过相同名字获取的日志管理器也都指向同一个对象.</p><h3 id="获取日志管理器"><a href="#获取日志管理器" class="headerlink" title="获取日志管理器"></a>获取日志管理器</h3><p>可以通过<code>LogManager</code>类提供的静态方法获取这个全局唯一的日志管理器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> LogManager <span class="title">getLogManager</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><h3 id="日志管理器配置属性"><a href="#日志管理器配置属性" class="headerlink" title="日志管理器配置属性"></a>日志管理器配置属性</h3><p>日志管理器对象在类初始化时创建，初始化时会默认读取JDK目录下<code>jre/lib/logging.properties</code>配置文件来获取默认的配置，可以在运行Java程序时指定属性<code>java.util.logging.config.file</code>的值来更改默认配置文件的位置，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">java -Djava.util.logging.config. file=...</span><br></pre></td></tr></table></figure><p>或者，可以调用<code>LogManager</code>对象的<code>readConfiguration(InputStream in)</code>方法来重新初始化默认配置，自JDK 9 起，这个方法被<code>updateConfiguration()</code>取代.</p><h2 id="日志i18n国际化"><a href="#日志i18n国际化" class="headerlink" title="日志i18n国际化"></a>日志i18n国际化</h2><hr><p>在获取日志记录器的方法<code>Logger.getLogger()</code>方法中，提供了第二个参数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Logger <span class="title">getLogger</span><span class="params">(String name, String resourceBundleName)</span></span>;</span><br></pre></td></tr></table></figure><p><code>resourceBundleName</code>指向一个资源文件，其中定义了相应语言的对照文本，例如现在创建了某个语言资源包<code>i18n/log_zh.properties</code>和<code>i18n/log_en.properties</code>，内容分别为：</p><figure class="highlight properties"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># log_zh.properties文件内容</span></span><br><span class="line"><span class="attr">login</span>=<span class="string">登录</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># log_en.properties文件内容</span></span><br><span class="line"><span class="attr">login</span>=<span class="string">sign in</span></span><br></pre></td></tr></table></figure><p>现在再来打印一下日志：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Logger logger = Logger.getLogger(<span class="string">""</span>, <span class="string">"i18n.log"</span>);</span><br><span class="line">logger.info(<span class="string">"login"</span>);<span class="comment">// 日志内容 login 将被转换为 登录</span></span><br></pre></td></tr></table></figure><p>此外，资源包中的内容还可以添加占位符({0}, {1}…)，例如：</p><figure class="highlight properties"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">login</span>=<span class="string">欢迎您，&#123;0&#125;</span></span><br></pre></td></tr></table></figure><p>现在打印一下带参数的日志：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Logger logger = Logger.getLogger(<span class="string">""</span>, <span class="string">"i18n.log"</span>);</span><br><span class="line">logger.log(Level.INFO, <span class="string">"login"</span>, <span class="string">"zylyye"</span>);<span class="comment">// 日志内容被替换为 欢迎您，zylyye</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在编写程序时，经常需要使用输出工具（例如常用的&lt;code&gt;System.out.println()&lt;/code&gt;）来输出、记录或者对比某些变量的值，在程序的运行期间，需要记录一些关键日志信息来调试、监控或者方便以后排查问题，此时，一般输出语句无法满足日志记录的需求，因此，Java内部提供了一套用于日志记录的API.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>Java异常与断言</title>
    <link href="http://zylyye.cn/2018/07/02/java-base/java-exception-and-assert/"/>
    <id>http://zylyye.cn/2018/07/02/java-base/java-exception-and-assert/</id>
    <published>2018-07-02T11:41:26.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>程序执行时，运行环境的许多因素会影响到程序的正常运行，例如网络是否连接、文件是否存在等等，Java中使用异常机制来帮助程序编写者处理这些理想之外的意外情况.</p><a id="more"></a><h2 id="Java中异常类的层次结构"><a href="#Java中异常类的层次结构" class="headerlink" title="Java中异常类的层次结构"></a>Java中异常类的层次结构</h2><hr><p>Java中预定义异常的主要机构：</p><pre class="mermaid">graph BT    Error -.-> Throwable    Exception -.-> Throwable    IOException -.-> Exception    RuntimeException -.-> Exception</pre><p>所有的异常类都继承自<code>Throwable</code>类，<code>Error</code>与<code>Exception</code>是<code>Throwable</code>的直接子类，其中<code>Error</code>异常一般是在系统发生内部错误时抛出.</p><p><code>Exception</code>下又有两大常见的异常<code>RuntimeException</code>和<code>IOException</code>，其中：</p><p><strong><code>RuntimeException</code>即运行时异常，通常在程序本身发生错误时自动抛出，例如：</strong></p><ul><li>访问一个对象的方法时，对象为<code>null</code>，抛出空指针异常<code>NullPointerException</code></li><li>访问数组的下标超过了数组的实际长度，抛出数组下标越界异常<code>ArrayIndexOutOfBoundsException</code></li><li>错误的进行强制类型转换，抛出类型转换异常<code>ClassCastException</code></li></ul><p><strong><code>IOException</code>是程序运行时，由于外界因素导致的I/O操作异常，例如：</strong></p><ul><li>访问的文件不存在，抛出<code>FileNotFoundException</code>异常</li><li>到达文件结尾，继续读取抛出<code>EOFException</code>异常</li></ul><h3 id="checked与unchecked异常"><a href="#checked与unchecked异常" class="headerlink" title="checked与unchecked异常"></a>checked与unchecked异常</h3><p>Java中分为checked与unchecked两种异常，其中unchecked异常可以不进行处理，而checked异常必须进行捕获处理(或者向上层抛出)</p><ul><li><code>Error</code>与<code>RuntimeException</code>异常均属于unchecked异常，程序中不强制对它们进行处理</li><li>除此之外的异常属于checked的异常，这些异常必须被处理</li></ul><h2 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h2><p>在进行方法调用时，如果方法声明了可能会抛出unchecked异常，则需要进行异常捕获或者继续向上抛出.</p><h3 id="异常捕获"><a href="#异常捕获" class="headerlink" title="异常捕获"></a>异常捕获</h3><p>Java中使用try语句包围可能发生异常的语句，再使用catch子句对相应类型的异常进行捕获：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">File file = <span class="keyword">new</span> File(<span class="string">"demo.txt"</span>);</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    file.createNewFile();</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    <span class="comment">// 打印堆栈轨迹</span></span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>catch中明确表明需要捕获的异常类型，这样可以捕获声明的异常类型及其子类型，如果需要，可以使用多个catch子句来捕获不同的异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    FileInputStream fileInputStream = <span class="keyword">new</span> FileInputStream(<span class="string">"demo.txt"</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (FileNotFoundException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用多个catch子句时，需要注意，当方法抛出异常时，会根据catch中声明的异常类型，按照定义的顺序，挨个寻找异常处理器，一旦寻找到合适的处理器，就立即应用而不再向后继续搜寻，因此，越具体的异常，应该越靠前进行声明.</p><p>除此之外，自JDK 7起，如果对待多个异常，希望使用相同的逻辑来处理，可以在catch子句中声明多个异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    exceptionTest();</span><br><span class="line">&#125; <span class="keyword">catch</span> (ExceptionA | ExceptionB e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="finally子句"><a href="#finally子句" class="headerlink" title="finally子句"></a>finally子句</h3><p>当代码中发生异常时，异常发生位置后面的语句不再运行，此时程序直接跳转到相应的catch子句中. 如果希望有些语句始终会被执行，而不会受异常影响(例如释放被占用的资源)，可以使用finally子句：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">FileInputStream fileInputStream = <span class="keyword">null</span>;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    fileInputStream = <span class="keyword">new</span> FileInputStream(<span class="string">"demo.txt"</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (fileInputStream != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">// 关闭文件输入流</span></span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            fileInputStream.close();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="comment">// ignore this</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用finally子句，需要留意几点：</p><ul><li>try语句可以只包含finally子句，而没有catch子句</li><li>在一个try语句块中，finally始终最后执行</li><li>如果finally中有返回语句，则其返回的值会始终覆盖try语句中的返回值</li><li>如果finally中抛出异常，则会覆盖try或者catch语句中抛出的异常</li></ul><h3 id="带资源的try语句"><a href="#带资源的try语句" class="headerlink" title="带资源的try语句"></a>带资源的try语句</h3><p>我们经常使用try/finally组合语句来打开和关闭一个资源的输入输出流：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// open a I/O stream</span></span><br><span class="line">&#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">    <span class="comment">// close the opened I/O stream</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样的结构可以保证资源被正确的释放，自JDK 7起，可以使用带资源的try语句来简化这一点：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> (IOStream stream = ...) &#123;</span><br><span class="line">    <span class="comment">// do something with stream</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>使用带资源的try语句时，资源会自动的被关闭(即资源的<code>close()</code>方法会自动的被调用)，但前提是，<strong>这个资源类实现了<code>AutoCloseable</code>方法</strong>，例如对于之前打开文件输入流的例子，现在可以使用带资源try语句对其进行简化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> (FileInputStream fileInputStream = <span class="keyword">new</span> FileInputStream(<span class="string">"demo.txt"</span>)) &#123;</span><br><span class="line">    <span class="comment">// do something</span></span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于带资源的try语句，当程序离开try语句时，会自动调用资源的<code>close()</code>方法，还有一点与一般try语句不同的是，带资源try语句后的catch语句同样也可以用来捕获<code>close()</code>方法执行时抛出的异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> (FileInputStream fileInputStream = <span class="keyword">new</span> FileInputStream(<span class="string">"demo.txt"</span>)) &#123;</span><br><span class="line">    <span class="comment">// do something</span></span><br><span class="line">&#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">    <span class="comment">// 如果自动关闭时，fileInputStream.close()方法抛出异常，则会在这里捕获</span></span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>还有一种情况，如果try语句也同时抛出了异常，调用<code>close()</code>方法抛出的异常不会覆盖try语句中待抛出的异常，而是被自动的添加到原来异常的suppressed的异常列表中(异常对象有个<code>addSuppressed()</code>方法，可通过该方法向目标异常中添加<code>suppressed</code>异常).</p><h3 id="向上抛出异常"><a href="#向上抛出异常" class="headerlink" title="向上抛出异常"></a>向上抛出异常</h3><p>有时候，发生异常时不应该立即捕获处理，应该交由调用者来决定如果处理该异常，此时可以将异常向上抛出. 此时只需要在方法后声明可能抛出的异常列表即可(多个异常使用<code>,</code>隔开)：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">method</span><span class="params">()</span> <span class="keyword">throws</span> ExceptionA, ExceptionB </span>&#123;</span><br><span class="line">    <span class="comment">// method body</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="自定义异常与手动抛出异常"><a href="#自定义异常与手动抛出异常" class="headerlink" title="自定义异常与手动抛出异常"></a>自定义异常与手动抛出异常</h2><hr><h3 id="自定义异常"><a href="#自定义异常" class="headerlink" title="自定义异常"></a>自定义异常</h3><p>Java预定义的异常经常不能满足实际的业务需求，此时可以根据需要进行自定义异常.</p><p>自定义异常时需要继承<code>Throwable</code>，通常的自定义异常都直接实现<code>Exception</code>或<code>RuntimeException</code>：</p><ul><li>如果自定义的异常需要强制被处理，则可以继承<code>Exception成为checked异常</code></li><li>如果不需要强制处理，则可以继承<code>RuntimeException</code>成为unchecked异常</li></ul><p>例如自定义一个网络连接失败异常：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NetworkConnectedFailedException</span> <span class="keyword">extends</span> <span class="title">Exception</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">NetworkConnectedFailedException</span><span class="params">(String message)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(message);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="手动抛出异常"><a href="#手动抛出异常" class="headerlink" title="手动抛出异常"></a>手动抛出异常</h3><p>当发生错误时，我们可以根据需要手动抛出一个异常，来通知调用者进行处理，Java中可以使用<code>throw</code>关键字来抛出一个异常对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (time &gt; MAX_CONNECT_TIME) &#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> NetworkConnectedFailedException(<span class="string">"网络连接超时"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当然，在一个方法中手动抛出一个异常后，需要在方法后声明将会抛出的异常列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">connect</span><span class="params">()</span> <span class="keyword">throws</span> NetworkConnectedFailedException </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (time &gt; MAX_CONNECT_TIME) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> NetworkConnectedFailedException(<span class="string">"网络连接超时"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="包装异常"><a href="#包装异常" class="headerlink" title="包装异常"></a>包装异常</h2><hr><p>有时候，我们的方法中不需要关注底层抛出了何种异常，而应该将底层的异常根据业务需要，封装成我们自己的异常，对一场进行二次包装，变成符合我们自己需要的异常即为<strong>包装异常</strong>，包装异常的意义在于，我们想要抛出自己的异常，同时又不想丢失引起异常的信息. 通过异常对象的<code>initCause(Throwable cause)</code>方法即可将原始异常存入新建的异常中，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">open</span><span class="params">()</span> <span class="keyword">throws</span> OpenFileFailedException </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> (FileInputStream fileInputStream = <span class="keyword">new</span> FileInputStream(<span class="string">"demo.txt"</span>)) &#123;</span><br><span class="line">        <span class="comment">// do something</span></span><br><span class="line">    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">        OpenFileFailedException exception = <span class="keyword">new</span> OpenFileFailedException(<span class="string">"文件打开失败"</span>);</span><br><span class="line">        exception.initCause(e);</span><br><span class="line">        <span class="keyword">throw</span> exception;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="断言"><a href="#断言" class="headerlink" title="断言"></a>断言</h2><hr><p>异常的存在是为了帮助我们处理程序运行期间发生的意外情况，而断言是为了声明程序能够正确执行的先决条件. <strong>断言只用于程序的开发测试阶段.</strong></p><h3 id="使用断言"><a href="#使用断言" class="headerlink" title="使用断言"></a>使用断言</h3><p>Java中使用<code>assert</code>关键字来声明断言，有两种格式的断言</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">assert</span> condition;</span><br><span class="line"><span class="keyword">assert</span> condition : expression;</span><br></pre></td></tr></table></figure><p>断言声明语句中，<code>condition</code>是一个条件表达式，该表达式返回<code>true</code>或者<code>false</code>， 返回true，则表示符合条件，程序继续向下执行，返回false则会抛出<code>AssertionError</code>异常.</p><p><code>expression</code>用于定义错误信息，它可以是字符串，或者能够转化为字符串的对象.</p><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getSalary</span><span class="params">(<span class="keyword">int</span> hours)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">assert</span> hours &gt; <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 如果需要更详细的提示，使用下面这种格式的断言</span></span><br><span class="line">    <span class="comment">// assert hours &gt; 0 : "时间不能为负数: " + hours;</span></span><br><span class="line">    <span class="keyword">return</span> hours * <span class="number">10</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>注意：断言默认是关闭的，需要在程序运行期间指定虚拟机参数<code>-ea</code>来打开断言(如使用<code>java -ea</code>来执行程序).</strong></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;程序执行时，运行环境的许多因素会影响到程序的正常运行，例如网络是否连接、文件是否存在等等，Java中使用异常机制来帮助程序编写者处理这些理想之外的意外情况.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>Java代理对象</title>
    <link href="http://zylyye.cn/2018/06/30/java-base/java-object-proxy/"/>
    <id>http://zylyye.cn/2018/06/30/java-base/java-object-proxy/</id>
    <published>2018-06-30T08:49:25.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>Java代理对象为对象的方法调用带来的极大的灵活性，通过使用代理对象，我们可以实现对象的方法跟踪、过滤，甚至动态实现某个具体的接口.</p><a id="more"></a><h2 id="创建代理对象"><a href="#创建代理对象" class="headerlink" title="创建代理对象"></a>创建代理对象</h2><hr><p>Java反射包中有一个提供了代理对象相关功能的类<code>java.lang.reflect.Proxy</code>，该类提供了创建代理对象的静态方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">newProxyInstance</span><span class="params">(ClassLoader loader, Class&lt;?&gt;[] interfaces, InvocationHandler h)</span></span>;</span><br></pre></td></tr></table></figure><p>该方法接收三个参数：</p><ul><li><code>ClassLoader loader</code>  提供一个类加载器用于生成代理类的Class对象</li><li><code>Class&lt;?&gt;[] interfaces</code> 代理类要实现的接口</li><li><code>InvocationHandler h</code>   自定义的方法调用处理器</li></ul><p>当需要为某个类创建代理时，需要提供该类实现的接口以及相应的方法调用处理器(类加载器可以使用默认类加载器)，调用代理对象中的方法时，只能调用接口中的方法.</p><h2 id="自定义方法调用处理器"><a href="#自定义方法调用处理器" class="headerlink" title="自定义方法调用处理器"></a>自定义方法调用处理器</h2><hr><p>自定义方法调用处理器需实现<code>InvocationHandler</code>接口，该接口中只有一个抽象方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable</span>;</span><br></pre></td></tr></table></figure><p>当调用代理对象中的方法时，会将代理对象、调用方法以及提供的参数传递给该方法，如果需要代理某个具体的对象，则需要将其保存在自定义方法处理器中，以便调用.</p><h2 id="简单的代理对象示例"><a href="#简单的代理对象示例" class="headerlink" title="简单的代理对象示例"></a>简单的代理对象示例</h2><hr><p>例如对于这样一个接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Connector</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">connect</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">disconnect</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在想要动态创建一个该接口的实现：接口中的方法被调用时，打印日志：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 自定义的方法调用处理器</span></span><br><span class="line">InvocationHandler handler = <span class="keyword">new</span> InvocationHandler() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">                System.out.println(method.getName());</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line"><span class="comment">// 创建Connector的代理对象</span></span><br><span class="line">Connector connector = (Connector) Proxy.newProxyInstance(ProxyDemo.class.getClassLoader(), <span class="keyword">new</span> Class[]&#123;Connector.class&#125;, handler);</span><br><span class="line"><span class="comment">// 像Connector子类一样调用代理对象的方法</span></span><br><span class="line">connector.connect();<span class="comment">// 输出connect</span></span><br><span class="line">connector.disconnect();<span class="comment">// 输出disconnect</span></span><br></pre></td></tr></table></figure><h2 id="使用CGLIB创建代理对象"><a href="#使用CGLIB创建代理对象" class="headerlink" title="使用CGLIB创建代理对象"></a>使用CGLIB创建代理对象</h2><hr><h3 id="内置代理对象的缺点"><a href="#内置代理对象的缺点" class="headerlink" title="内置代理对象的缺点"></a>内置代理对象的缺点</h3><p>Java内置的<code>Proxy</code>主要适用于动态创建某个接口的具体实现，创建代理对象时，必须要给出接口，因此，创建某个类的代理对象时，就必须使其继承某个接口，然后基于接口中定义的方法，来进行代理.</p><p>当我们需要创建某个类的代理对象时，或者动态创建某个类的子类时，Java内置的代理对象创建工具并不方便.</p><h3 id="CGLIB"><a href="#CGLIB" class="headerlink" title="CGLIB"></a>CGLIB</h3><p><code>CGLIB</code>(Code Generation Library) 是一个代码生成库，通过该库提供的工具，可以帮助我们直接创建某个类的代理对象或者动态创建一个类的子类.</p><p><code>CGLIB</code>的项目地址：<a href="https://github.com/cglib/cglib">CGLIB</a>，使用前，需要先下载相关jar包.</p><h3 id="使用CGLIB创建代理对象-1"><a href="#使用CGLIB创建代理对象-1" class="headerlink" title="使用CGLIB创建代理对象"></a>使用CGLIB创建代理对象</h3><p>例如对于这样的类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ApiConnector</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">connect</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"connected"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">disconnect</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"disconnected"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在使用<code>CGLIB</code>库中提供的方法创建代理对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">Enhancer enhancer = <span class="keyword">new</span> Enhancer();</span><br><span class="line">enhancer.setSuperclass(ApiConnector.class);</span><br><span class="line">enhancer.setCallback(<span class="keyword">new</span> Callback() &#123;&#125;);</span><br><span class="line">enhancer.setCallback(<span class="keyword">new</span> MethodInterceptor() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">intercept</span><span class="params">(Object o, Method method, Object[] objects, MethodProxy methodProxy)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">        System.out.println(method.getName());</span><br><span class="line">        <span class="comment">// 调用原对象的方法</span></span><br><span class="line">        methodProxy.invokeSuper(o, objects);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line">ApiConnector apiConnector = (ApiConnector) enhancer.create();</span><br><span class="line">apiConnector.connect();</span><br><span class="line">apiConnector.disconnect();</span><br><span class="line"><span class="comment">// 输出内容：</span></span><br><span class="line"><span class="comment">// connect</span></span><br><span class="line"><span class="comment">// connected</span></span><br><span class="line"><span class="comment">// disconnect</span></span><br><span class="line"><span class="comment">// disconnected</span></span><br></pre></td></tr></table></figure><p>不像Java内置的代理对象，使用<code>CGLIB</code>可以创建类或接口的代理对象，并且在类的代理对象中，还可以方便的调用原对象的方法.且不同于内置的代理对象，<code>CGLIB</code>中的代理对象更像一个对象拦截器.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Java代理对象为对象的方法调用带来的极大的灵活性，通过使用代理对象，我们可以实现对象的方法跟踪、过滤，甚至动态实现某个具体的接口.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
      <category term="代理对象" scheme="http://zylyye.cn/tags/%E4%BB%A3%E7%90%86%E5%AF%B9%E8%B1%A1/"/>
    
  </entry>
  
  <entry>
    <title>函数式接口与lambda表达式</title>
    <link href="http://zylyye.cn/2018/05/15/java-base/java_functional-interface-and-lambda-expression/"/>
    <id>http://zylyye.cn/2018/05/15/java-base/java_functional-interface-and-lambda-expression/</id>
    <published>2018-05-15T13:43:03.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="函数式接口"><a href="#函数式接口" class="headerlink" title="函数式接口"></a>函数式接口</h2><hr><p>函数式接口的定义很简单：<strong>即只包含一个抽象方法的接口</strong>.</p><a id="more"></a><p>例如一个简单的函数式接口：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Sumable</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>函数式接口中可以包含多个方法，但<strong>只能有一个抽象方法</strong>，因此，其它方法必须给出默认实现，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@FunctionalInterface</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Sumable</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">sum</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">default</span> <span class="keyword">int</span> <span class="title">thenAdd</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通常在函数式接口上都会添加一个<code>@FunctionalInterface</code>注解，这种注解称为<strong>标记注解</strong>，用来标记这个接口为函数式接口，这个注解不是必须的，但当自定义函数式接口时，建议添加.</p><h2 id="lambda表达式"><a href="#lambda表达式" class="headerlink" title="lambda表达式"></a>lambda表达式</h2><hr><p>lambda表达式更像是一个简写的匿名内部类，例如现在有这样一个方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ToolKit</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">getSum</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b, Sumable s)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> s.sum(a,b);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其中<code>getSum()</code>方法依赖<code>Sumable</code>接口中的<code>sum()</code>方法来完成对参数<code>a</code>、<code>b</code>的运算，我们可以传递一个匿名内部类来应用一个简单的运算：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">ToolKit.getSum(<span class="number">1</span>, <span class="number">3</span>, <span class="keyword">new</span> Sumable() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">sum</span> <span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>而这个过程可以使用lambda表达式来简化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ToolKit.getSum(<span class="number">1</span>,  <span class="number">3</span>, (<span class="keyword">int</span> a, <span class="keyword">int</span> b) -&gt; &#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>实际上，在lambda表达式中接收参数的类型可以自动的被编译器推导出，因此参数的类型可以省略：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ToolKit.getSum(<span class="number">1</span>, <span class="number">3</span>, (a, b) -&gt; &#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>对于如此简单的计算，还可以进一步简化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ToolKit.getSum(<span class="number">1</span>, <span class="number">3</span>, (a, b) -&gt; a + b);</span><br></pre></td></tr></table></figure><h3 id="lambda表达式的语法"><a href="#lambda表达式的语法" class="headerlink" title="lambda表达式的语法"></a>lambda表达式的语法</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">() -&gt; &#123;&#125;</span><br></pre></td></tr></table></figure><p>lambda表达式的写法看起来像是一个函数，括号<code>()</code>中定义要接收的参数列表，符号<code>-&gt;</code>后紧跟着要执行的代码块.</p><ul><li><p><strong>如果符号<code>-&gt;</code>后面的代码块中只有一个表达式，那么这个lambda表达式就会返回这个表达式的值</strong>，正如最终简化的<code>ToolKit.getSum()</code>方法调用示例里的那样：</p>  <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ToolKit.getSum(<span class="number">1</span>, <span class="number">3</span>, (a, b) -&gt; a + b);</span><br></pre></td></tr></table></figure></li><li><p><strong>如果<code>-&gt;</code>中的执行语句较多，则应该使用<code>{}</code>来包围形成代码块</strong></p></li><li><p><strong>一般情况下，编译器可以根据对应的接口得出参数的类型，因此，参数类型可以省略不写</strong></p></li><li><p><strong>即使是空参数列表，括号()也不能省去，但如果只有一个参数，则可以省去括号</strong>，例如：</p>  <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arg -&gt; arg + <span class="number">1</span></span><br></pre></td></tr></table></figure></li><li><p><strong>除了可以像参数一样直接传递给其他方法，lambda表达式还可以用来直接构造函数式接口的子类对象</strong>，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Sumable sumable = (a, b) -&gt; a + b;</span><br></pre></td></tr></table></figure></li></ul><h3 id="lambda的方法引用与构造器引用"><a href="#lambda的方法引用与构造器引用" class="headerlink" title="lambda的方法引用与构造器引用"></a>lambda的方法引用与构造器引用</h3><h4 id="方法引用"><a href="#方法引用" class="headerlink" title="方法引用"></a>方法引用</h4><p>使用lambda表达式可以使用符号<code>::</code>直接传递其他类的方法，有三种传递类型，例如对于这样一个类：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">class LamFunc&#123;</span><br><span class="line">    public void func(int arg)&#123;</span><br><span class="line">        // do something</span><br><span class="line">    &#125;</span><br><span class="line">    public static void staticFunc(int arg)&#123;</span><br><span class="line">        // do something</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol><li><p><strong>对象::方法名</strong></p> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> LamFunc()::func</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">x-&gt;<span class="keyword">new</span> LamFunc().func(x)</span><br></pre></td></tr></table></figure></li><li><p><strong>类名::静态方法名</strong></p> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">LamFunc::staticFunc</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">x-&gt;LamFunc.staticFunc(x)</span><br></pre></td></tr></table></figure></li><li><p><strong>类名::静态方法名</strong></p> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">LamFunc::func;</span><br><span class="line"><span class="comment">// 相当于</span></span><br><span class="line">(lamFunc,x)-&gt;lamFunc.func(x)</span><br></pre></td></tr></table></figure></li></ol><h4 id="构造器引用"><a href="#构造器引用" class="headerlink" title="构造器引用"></a>构造器引用</h4><p>与方法引用类似，构造器引用写法为：<strong>类名::new</strong></p><p>该lambda表达式将会得到一个该类的对象.</p><p>如果方法或构造方法重载，那传入该方法时，编译器将会自动根据参数列表自动解析适配.</p><p>数组也是类，因此也可以传递数组的构造方法,例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[]::<span class="keyword">new</span></span><br><span class="line">String[]::<span class="keyword">new</span></span><br></pre></td></tr></table></figure><h3 id="lambda表达式的作用域"><a href="#lambda表达式的作用域" class="headerlink" title="lambda表达式的作用域"></a>lambda表达式的作用域</h3><p>lambda表达式与一个匿名内部类很相似，其作用域规则与内部类的规则一致，唯一不同的是，<strong>lambda表达式只是一个方法体，其内部的<code>this</code>关键字指向外部类对象的引用.</strong></p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;函数式接口&quot;&gt;&lt;a href=&quot;#函数式接口&quot; class=&quot;headerlink&quot; title=&quot;函数式接口&quot;&gt;&lt;/a&gt;函数式接口&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;函数式接口的定义很简单：&lt;strong&gt;即只包含一个抽象方法的接口&lt;/strong&gt;.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
      <category term="lambda" scheme="http://zylyye.cn/tags/lambda/"/>
    
  </entry>
  
  <entry>
    <title>Java反射</title>
    <link href="http://zylyye.cn/2018/05/12/java-base/java-reflection/"/>
    <id>http://zylyye.cn/2018/05/12/java-base/java-reflection/</id>
    <published>2018-05-12T14:01:48.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<hr><p>通过使用Java反射，我们可以在程序运行期间获取任意类的属性及方法、动态的创建它们的对象、动态的访问它们的属性与方法.</p><a id="more"></a><h2 id="Class类"><a href="#Class类" class="headerlink" title="Class类"></a>Class类</h2><hr><p>Java中有一个专门用于存储某个类的内部信息的对象，这个对象就是Class对象，Class类中定义了许多有关反射的方法，该类没有公共构造方法，只能在运行时被Java虚拟机自动构建，Java虚拟机会为每个被加载的类构建一个相应的Class类的对象.</p><h3 id="获取类的Class对象"><a href="#获取类的Class对象" class="headerlink" title="获取类的Class对象"></a>获取类的Class对象</h3><p>对于一个已经被Java虚拟机加载的类，Java中提供了两种用于获取其Class对象的方法：</p><ol><li><p><code>**ClassName.class</code>** 知道一个类的类名，就可以使用<code>.class</code>来获取该类的Class对象，如<code>String.class</code></p></li><li><p><strong>通过对象方法<code>getClass()</code></strong> 借助超类中的<code>getClass()</code>方法，可以获取对象所属类的Class对象，如<code>“”.getClass()</code></p></li></ol><p>对于一个未加载的类，可以使用Class类提供的静态方法来手动加载并获取该类的Class对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 手动加载Thread类并获取其Class对象</span></span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    Class threadClass = Class.forName(<span class="string">"java.lang.Thread"</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该方法定义如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Class&lt;?&gt; forName(String className);</span><br></pre></td></tr></table></figure><p>使用该方法来手动加载类时，需要提供其类名及完整包名，且需要保证该类在当前程序能访问的类路径下，否则，该方法将抛出<code>ClassNotFoundException</code>异常.</p><p>Java虚拟机只会为一个类构建一个Class对象，因此，无论通过何种方式获取的某一个类的Class对象，它们都指向同一个对象(即使使用<code>Class.forName()</code>方法加载多次，得到的依然是同一个Class对象)，即：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Class.forName(<span class="string">"java.lang.Thread"</span>) == Thread.class; <span class="comment">// true</span></span><br><span class="line"><span class="keyword">new</span> Thread().getClass() == Thread.class; <span class="comment">// true</span></span><br></pre></td></tr></table></figure><h2 id="利用反射机制访问类的内部信息"><a href="#利用反射机制访问类的内部信息" class="headerlink" title="利用反射机制访问类的内部信息"></a>利用反射机制访问类的内部信息</h2><hr><h3 id="获取构造方法"><a href="#获取构造方法" class="headerlink" title="获取构造方法"></a>获取构造方法</h3><p>Class类中有四个有关获得构造方法信息的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获得某个含有某个类的构造方法的数组</span></span><br><span class="line"><span class="keyword">public</span> Constructor&lt;?&gt;[] getConstructors();</span><br><span class="line"><span class="keyword">public</span> Constructor&lt;?&gt;[] getDeclaredConstructors();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 提供参数类型，获得制定的类构造方法</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Constructor&lt;T&gt; <span class="title">getConstructor</span><span class="params">(Class&lt;?&gt;... parameterTypes)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> Constructor&lt;T&gt; <span class="title">getDeclaredConstructor</span><span class="params">(Class&lt;?&gt;... parameterTypes)</span></span>;</span><br></pre></td></tr></table></figure><p><strong>其中拥有Declard的方法与没有的区别是，Declared包含该类的全部方法，没有Declared只会给出可见性为public的方法</strong>.</p><p>这些方法返回的都是Constructor类的对象或对象数组，该类包含了获得构造方法名及参数等方法.</p><h3 id="获取方法"><a href="#获取方法" class="headerlink" title="获取方法"></a>获取方法</h3><p>Class类中有四个获得有关类中方法信息的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获得含有类中方法的数组</span></span><br><span class="line"><span class="keyword">public</span> Method[] getMethods();</span><br><span class="line"><span class="keyword">public</span> Method[] getDeclaredMethods();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 提供方法名与方法参数，获得指定的方法</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Method <span class="title">getMethod</span><span class="params">(String name, Class&lt;?&gt;... parameterTypes)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> Method <span class="title">getDeclaredMethod</span><span class="params">(String name, Class&lt;?&gt;... parameterTypes)</span></span>;</span><br></pre></td></tr></table></figure><p><strong>同样，含有Declared的方法会给出所有方法，但不会包含从父类继承过来的方法.</strong></p><p>Method类对象包含了获得方法名与参数等方法.</p><h3 id="获取数据域"><a href="#获取数据域" class="headerlink" title="获取数据域"></a>获取数据域</h3><p>Class类中的四个有关获得类中数据域信息的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 获得含有数据域的数组</span></span><br><span class="line"><span class="keyword">public</span> Field[] getFields();</span><br><span class="line"><span class="keyword">public</span> Field[] getDeclaredFields();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 提供变量名，获得指定数据域</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Field <span class="title">getField</span><span class="params">(String name)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> Field <span class="title">getDeclaredField</span><span class="params">(String name)</span></span>;</span><br></pre></td></tr></table></figure><p>其中<code>Field</code>对象中包含了获取变量名，类型及存储值等方法.</p><h3 id="获取成员修饰符及参数等信息"><a href="#获取成员修饰符及参数等信息" class="headerlink" title="获取成员修饰符及参数等信息"></a>获取成员修饰符及参数等信息</h3><p><code>Constructor</code>类与<code>Method</code>类都实现了<code>GenericDeclaration</code>与<code>Member</code>接口，<code>GenericDeclaration</code>接口定义了获得参数信息的方法，<code>Member</code>接口定义了获得成员名与修饰符名的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// GenericDeclaration接口中定义的相关方法</span></span><br><span class="line">TypeVariable&lt;?&gt;[] getTypeParameters();</span><br><span class="line"></span><br><span class="line"><span class="comment">// Member接口中定义的相关方法</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">getModifiers</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function">String <span class="title">getName</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><h4 id="获取修饰符"><a href="#获取修饰符" class="headerlink" title="获取修饰符"></a>获取修饰符</h4><p>成员的修饰符即类似于<code>public static</code>这样的关键字序列，Member接口中的<code>getModifiers()</code>方法返回用于描述修饰符的整型，可以使用<code>Modifier</code>类的静态方法将其转换为字符串描述：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">(<span class="keyword">int</span> modifier)</span></span>;</span><br></pre></td></tr></table></figure><h4 id="获取参数信息"><a href="#获取参数信息" class="headerlink" title="获取参数信息"></a>获取参数信息</h4><p><code>GenericDeclaration</code>接口中的<code>getTypeParameters()</code>方法返回包含参数信息的<code>TypeVariable</code>数组，可以使用类中的<code>getName()</code>方法获得参数类型名，此外，在<code>Constructor</code>和<code>Method</code>类中还有<code>getParameterCount()</code>方法用于获得参数个数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getParameterCount</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><h4 id="获取返回类型"><a href="#获取返回类型" class="headerlink" title="获取返回类型"></a>获取返回类型</h4><p><code>Method</code>类中提供了获取方法返回类型的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> Class&lt;?&gt; getReturnType();</span><br></pre></td></tr></table></figure><h4 id="获取数据域的值"><a href="#获取数据域的值" class="headerlink" title="获取数据域的值"></a>获取数据域的值</h4><p>Field类中提供了get方法，将相应的对象传递给这个方法，就可以获得对应域存储的值，该方法是一个通用方法，任何类型的值将被作为Object对象返回，例如对于这样的类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Demo</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以利用反射来获取Demo对象中<code>name</code>属性的值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Demo demo = <span class="keyword">new</span> Demo(<span class="string">"name"</span>, <span class="number">123</span>);</span><br><span class="line"><span class="comment">// 获取该属性的Fileld对象</span></span><br><span class="line">Field field = Demo.class.getField(<span class="string">"name"</span>);</span><br><span class="line"><span class="comment">// 将一个Demo对象传递给field的get()方法，获得该域存储的值</span></span><br><span class="line">String name = (String)field.get(demo);</span><br></pre></td></tr></table></figure><p>如果数据域存储的是原始类型，则调用该方法时，会自动将其包装成相应的包装类型，同时，Field中还定义了获取原始类型值的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getInt</span><span class="params">(Object obj)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">long</span> <span class="title">getLong</span><span class="params">(Object obj)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">double</span> <span class="title">getDouble</span><span class="params">(Object obj)</span></span>;</span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>如果使用该方法访问私有的成员变量，则方法会抛出<code>IllegalAccessException</code>异常，但我们依然可以通过设置该域的可访问性来获取其值，Field类中提供了修改域可访问性的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAccessible</span><span class="params">(<span class="keyword">boolean</span> flag)</span></span>;</span><br></pre></td></tr></table></figure><p>例如访问Demo对象中的age值：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Field field = Demo.class.getDeclaredField(<span class="string">"age"</span>);</span><br><span class="line">field.setAccessible(<span class="keyword">true</span>);</span><br><span class="line"><span class="keyword">int</span> age = field.getInt(demo);</span><br></pre></td></tr></table></figure><h2 id="利用反射动态构造对象与调用方法"><a href="#利用反射动态构造对象与调用方法" class="headerlink" title="利用反射动态构造对象与调用方法"></a>利用反射动态构造对象与调用方法</h2><h3 id="动态构造对象"><a href="#动态构造对象" class="headerlink" title="动态构造对象"></a>动态构造对象</h3><p>Constructor类中定义了用于构造对象的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> T <span class="title">newInstance</span><span class="params">(Object... initargs)</span></span>;</span><br></pre></td></tr></table></figure><p>Constructor是一个泛型类，但我们将会只是简单的将构造好的对象转换为Object对象返回，例如，可以编写一个工具方法，接受一个Class对象，利用该对象获得相应类的空参数构造方法，然后利用空构造方法构造这个类的对象并返回：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tools</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">getInstance</span><span class="params">(Class cl)</span> </span>&#123;</span><br><span class="line">        Object obj = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Constructor cons = cl.getConstructor();</span><br><span class="line">            obj = cons.newInstance();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> obj;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="利用反射调用对象方法"><a href="#利用反射调用对象方法" class="headerlink" title="利用反射调用对象方法"></a>利用反射调用对象方法</h3><p>类似于Field对象中的<code>get()</code>方法，Method对象中有一个<code>invoke()</code>方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object obj, Object... args)</span></span>;</span><br></pre></td></tr></table></figure><ul><li>该方法的第一个参数是该Method所属的类的一个实例，后面的参数为方法接收的参数</li><li>如果这是一个静态方法，那么第一个参数可以为<code>null</code></li><li>如果该方法不接收参数，则可以只有第一个参数</li><li>该方法会返回原方法调用后返回的值(包装为Object对象),如果原方法无返回值，<code>invoke()</code>方法返回null</li></ul><h2 id="利用反射构建动态构建数组"><a href="#利用反射构建动态构建数组" class="headerlink" title="利用反射构建动态构建数组"></a>利用反射构建动态构建数组</h2><hr><p>在Java中，数组也是一个类，例如<code>int[]</code>,<code>String[]</code>等都有各自的Class对象，Class对象中提供了可以获得数组中元素类型的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> Class&lt;?&gt; getComponentType();</span><br></pre></td></tr></table></figure><p>利用该方法，对于任意一个给定的数组，我们都可以利用反射来获取其中存储的成员类型.</p><p>在<code>java.lang.reflect</code>包中有一个Array类，该类提供了创建指定类型数组的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">newInstance</span><span class="params">(Class&lt;?&gt; componentType, <span class="keyword">int</span> length)</span></span>;</span><br></pre></td></tr></table></figure><p>该方法的第一个参数表示待创建数组的类型，第二个参数表示带创建数组的长度.</p><p>利用该方法创建的数组都被包装为<code>Object</code>对象，我们需要手动进行类型转换,例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[] intArr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">10</span>];</span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line">intArr = (<span class="keyword">int</span>[]) Array.newInstance(<span class="keyword">int</span>.class, <span class="number">10</span>);</span><br><span class="line">String[] strArr = <span class="keyword">new</span> String[<span class="number">10</span>];</span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line">strArr = (String[]) Array.newInstance(String.class, <span class="number">10</span>);</span><br></pre></td></tr></table></figure><h3 id="创建多维数组"><a href="#创建多维数组" class="headerlink" title="创建多维数组"></a>创建多维数组</h3><p>该方法还提供了一个重载的方法，可以用来创建多维数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">newInstance</span><span class="params">(Class&lt;?&gt; componentType, <span class="keyword">int</span>... dimensions)</span></span>;</span><br></pre></td></tr></table></figure><p>该方法的第一个参数为多维数组中元素的类型，后面的参数依次为各个维度数组的长度，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[][] arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>][<span class="number">3</span>];</span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line">arr = (<span class="keyword">int</span>[][]) Array.newInstance(<span class="keyword">int</span>.class, <span class="number">2</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure><p>需要注意的时，<strong>通过该方法创建的数组维度不能超过255，否则会抛出<code>IllegalArgumentException</code>异常.</strong></p>]]></content>
    
    <summary type="html">
    
      &lt;hr&gt;
&lt;p&gt;通过使用Java反射，我们可以在程序运行期间获取任意类的属性及方法、动态的创建它们的对象、动态的访问它们的属性与方法.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
      <category term="Java反射" scheme="http://zylyye.cn/tags/Java%E5%8F%8D%E5%B0%84/"/>
    
  </entry>
  
  <entry>
    <title>Java继承类与实现接口</title>
    <link href="http://zylyye.cn/2018/05/07/java-base/java-extends-and-implements/"/>
    <id>http://zylyye.cn/2018/05/07/java-base/java-extends-and-implements/</id>
    <published>2018-05-07T07:01:30.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="类的继承"><a href="#类的继承" class="headerlink" title="类的继承"></a>类的继承</h2><hr><p>Java中可以通过继承来扩展一个类的功能，复用现有的方法，Java中使用<code>extends</code>关键字来进行继承，被继承的类称为父类，继承了其它类的类称为子类.</p><a id="more"></a><h3 id="定义子类"><a href="#定义子类" class="headerlink" title="定义子类"></a>定义子类</h3><p>定义一个普通的类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Parent</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Parent</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>现在使用<code>extends</code>来继承这个类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="comment">// add some methods here</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="继承的规则"><a href="#继承的规则" class="headerlink" title="继承的规则"></a>继承的规则</h3><ul><li>Java只能单继承，即只能同时继承一个类(但可以实现多个接口)</li><li>Java的继承为公有继承，子类中只能访问非私有成员(protected, public, 同一个包中的default)</li><li>子类中可以使用<code>super</code>关键字来访问父类的非私有成员及构造方法</li><li>子类不能直接访问或覆盖父类的私有成员，但可以通过父类提供的非私有方法间接访问</li><li>子类覆盖父类成员时，可以放大访问权限(如protect -&gt; public)</li></ul><h3 id="子类构造方法"><a href="#子类构造方法" class="headerlink" title="子类构造方法"></a>子类构造方法</h3><p>子类可以显示的使用<code>super</code>关键字来调用父类的构造方法，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> grade;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Childs</span><span class="params">(String name,<span class="keyword">int</span> age,<span class="keyword">int</span> grade)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name,age);</span><br><span class="line">        <span class="keyword">this</span>.grade = grade;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// add some new methods</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>子类的构造方法中，必须在第一行调用父类的构造方法，如果没有手动调用，则编译器会自动在构造方法中首行添加调用父构造方法的语句<code>super()</code>.</p><p><strong><code>super</code>关键子与<code>this</code>关键字类似，但有所区别，<code>this</code>关键字可以当做一个引用当前对象的变量使用，但<code>super</code>不行，例如在一个方法中，这样的语句是合法的：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Child self = <span class="keyword">this</span>;</span><br></pre></td></tr></table></figure><h3 id="覆盖父类的方法"><a href="#覆盖父类的方法" class="headerlink" title="覆盖父类的方法"></a>覆盖父类的方法</h3><p>有时候父类定义的方法不满足子类的需求，子类希望重写父类的方法，这时，子类可以定义一个与父类相同的方法来覆盖父类中的方法.</p><p>重写父类方法时，有如下限制:</p><ul><li>方法名必须相同</li><li>方法参数列表必须相同</li><li>方法返回类型需与原来的返回类型相兼容，即保持相同或者返回的类型为其子类</li><li>方法的返回权限只能保持一致或着更高，如原来是<code>private</code>,可以重写为<code>public</code></li></ul><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Parent</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"name:"</span>+name+<span class="string">"\nage:"</span>+age+<span class="string">"\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> grade;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Childs</span><span class="params">(String name,<span class="keyword">int</span> age,<span class="keyword">int</span> grade)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name,age);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"grade:"</span>+<span class="keyword">this</span>.grade+<span class="string">"\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>@Ovrride</code>注解表示该方法是为了覆盖了父类的方法，非必须.</p><p>在子类中可以使用<code>super</code>关键字来访问父类的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Parent</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> grade;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Childs</span><span class="params">(String name,<span class="keyword">int</span> age,<span class="keyword">int</span> grade)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name,age);</span><br><span class="line">        <span class="keyword">this</span>.grade = grade;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 访问父类的getInfo()方法</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">super</span>.getInfo()+<span class="string">"grade:"</span>+<span class="keyword">this</span>.grade+<span class="string">"\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果定义的方法只是与父类方法同名，但参数不同，则该方法会重载.</p><h3 id="使用final防止继承与重写"><a href="#使用final防止继承与重写" class="headerlink" title="使用final防止继承与重写"></a>使用final防止继承与重写</h3><p><code>final</code>关键字可以用在类上，也可以用在类中的变量或方法中.</p><p>如果用在类上，则这个类不能被继承，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> String &#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Java的预定义类<code>String</code>就是一个final类，该类不能被其他类继承.</p><p>如果<code>final</code>用在类中的变量或方法中，则该变量或方法不能被子类覆盖，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FinalClass</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> count;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">getCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line"><span class="keyword">return</span> <span class="keyword">this</span>.count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样的类中，<code>count</code>与<code>getCount</code>方法不能被子类覆盖.</p><p><strong>有关final的编译器优化</strong></p><p>虚拟机在调用一个对象的方法时，会提取该对象所属类的方法列表，来寻找合适的方法进行调用，如果该对象中没有找到，则从该类的父类中查找，这个搜索方法的过程会有一定的损耗，而如果将方法定义为private、static、final，则虚拟机无需进行这种方法检索而直接调用(这个过程由编译器进行优化，编译器会对这些方法进行<strong>静态绑定</strong>，而其它的无法在编译期间确定的编译器使用<strong>动态绑定</strong>的方式来生成调用指令).</p><p>此外，对于没有被继承且简短的方法，编译器会对其进行<strong>内联处理</strong>，从而提升方法的执行效率.</p><h2 id="抽象类"><a href="#抽象类" class="headerlink" title="抽象类"></a>抽象类</h2><hr><p>有时在设计一个类时，并不想给出方法的具体实现，或者希望把相似类的共同部分封装成一个类，共同方法的具体实现分别在各自的子类中再给出具体是实现，此时，可以定义一个抽象类来完成这个需求.</p><h3 id="抽象类与抽象方法"><a href="#抽象类与抽象方法" class="headerlink" title="抽象类与抽象方法"></a>抽象类与抽象方法</h3><p>如果只是声明了一个方法，给出了方法名，参数列表，却没有给出具体实现，而是希望子类能够自定义这个方法，这样的方法叫做抽象方法，抽象方法需要使用<code>abstract</code>关键字修饰，如果一个类中包含抽象方法，则这个类就是抽象类，也必须使用<code>abstract</code>关键字来修饰.例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">Connection</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String url;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Connection</span><span class="params">(String url)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.url = url;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">boolean</span> <span class="title">connect</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">close</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="抽象类的定义规则"><a href="#抽象类的定义规则" class="headerlink" title="抽象类的定义规则"></a>抽象类的定义规则</h3><ul><li>抽象类与普通类类似，在抽象类中可以定义抽象方法，抽象方法不能私有</li><li>抽象类不能实例化，因为可能存在没有具体实现的方法</li><li>抽象类中可以没有抽象方法</li><li>普通类继承了一个抽象类时，必须给出抽象类中所有抽象方法的具体实现</li><li>抽象类之间也可以相互继承，且可以按需实现部分抽象方法</li></ul><h2 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h2><hr><p>相比于抽象类，接口是更加抽象的特征集合，接口中只描述了某些类的公共特征，并不给出具体实现.</p><h3 id="定义接口"><a href="#定义接口" class="headerlink" title="定义接口"></a>定义接口</h3><p>定义一个类时使用<code>class</code>关键字，定义接口时使用<code>interface</code>关键字，与从抽象类不同的是，接口中定义的方法全部为抽象方法，此外还有以下规则：</p><ul><li>接口中定义的变量都必须是公共静态的常量，定义的方法都必须是公共抽象方法，在接口中定义变量或方法时，可以不用添加修饰符，编译器会自动为变量添加<code>public static final</code>，为抽象方法自动添加<code>public abstract</code></li><li>与类只能单继承不同，一个类可以实现多个接口，多个接口之间使用<code>,</code>分隔</li><li>接口之间可以使用<code>extends</code>继承</li><li>普通类实现一个接口时，必须实现其全部方法，抽象类可以按需只实现其中一部分</li><li>接口中可以使用<code>default</code>为抽象方法定义一个默认实现，子类在实现该接口时，拥有默认实现的方法，可以不用重写，保留默认实现</li></ul><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Connection</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> MAX_CONNECTED_COUNT = <span class="number">10</span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">connect</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">default</span> <span class="keyword">void</span> <span class="title">close</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.exit(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="接口默认方法冲突"><a href="#接口默认方法冲突" class="headerlink" title="接口默认方法冲突"></a>接口默认方法冲突</h3><p>由于接口中可以定义默认方法，且接口支持多实现，因此，可能会发生以下几种冲突问题：</p><ol><li><p><strong>子类实现的多个接口中，多个接口中存在同名方法，且都有默认实现</strong></p><p>此时子类中必须重写该方法以避免方法冲突问题</p></li><li><p><strong>子类实现的接口中与继承的类中有同名的方法，且接口中提供了默认实现</strong></p><p>此时父类中的方法优先，接口中的默认方法将被忽略，<strong>由此，如果在接口中定义与<code>Object</code>中同名方法的默认实现，则总会被<code>Object</code>类覆盖.</strong></p></li></ol><p>对于第一种情况，即使只有其中一个接口定义了默认实现，子类中依然会要求重写同名的方法，当然，可以在子类重写的方法中显示调用某个接口的默认实现，例如一个<code>Worker</code>类实现的接口中都包含<code>getAge()</code>方法，其中<code>Person</code>接口给了默认实现，现在想在子类中使用<code>Person</code>接口提供的默认实现：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Worker</span> <span class="keyword">implements</span> <span class="title">Person</span>, <span class="title">Employee</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> Person.<span class="keyword">super</span>.getAge();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="类的公共超类Object"><a href="#类的公共超类Object" class="headerlink" title="类的公共超类Object"></a>类的公共超类<code>Object</code></h2><hr><p>Java中定义的类会默认继承与一个预定义类<code>Object</code>，该类为所有类的公共超类，<code>Object</code>中定义了几个有用的方法.</p><h3 id="Object类中的常见方法"><a href="#Object类中的常见方法" class="headerlink" title="Object类中的常见方法"></a><code>Object</code>类中的常见方法</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">native</span> Class&lt;?&gt; getClass();</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">native</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">this</span> == obj);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">native</span> Object <span class="title">clone</span><span class="params">()</span> <span class="keyword">throws</span> CloneNotSupportedException</span>;</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> getClass().getName() + <span class="string">"@"</span> + Integer.toHexString(hashCode());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>除了这些方法之外，<code>Object</code>中还提供了与多线程相关的方法.</p><h3 id="对象克隆"><a href="#对象克隆" class="headerlink" title="对象克隆"></a>对象克隆</h3><p><code>Object</code>类中有一个用于对象克隆的方法<code>clone()</code>，该方法访问权限为<code>projected</code>，即无法在其他类中直接访问该方法来进行克隆方法，这么做的原因是默认实现的<code>clone()</code>方法是不安全的，它只会对这个对象进行<strong>浅拷贝</strong>，即如果对象中保存了其它对象的引用，那么克隆的对象只是简单拷贝了这个引用而非引用所指的对象.</p><p>为此，如果需要让一个对象提供克隆的功能，我们需要重新自定义这个<code>clone()</code>方法，并将该方法定义为<code>public</code>以供外部使用，Java为此定义了一个<code>Cloneable</code>接口，为了规范，一个类需要实现该接口并重写<code>clone()</code>方法来标识该类的对象对象克隆，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span> <span class="keyword">implements</span> <span class="title">Cloneable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> LocalDate start;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Student</span><span class="params">(String name,<span class="keyword">int</span> age)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">        start = LocalDate.now();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Student <span class="title">clone</span><span class="params">()</span> <span class="keyword">throws</span> CloneNotSupportedException </span>&#123;</span><br><span class="line">        Student student = (Student)<span class="keyword">super</span>.clone();</span><br><span class="line">        student.start = LocalDate.now();</span><br><span class="line">        <span class="keyword">return</span> student;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h2><hr><p><strong>多态</strong>可以理解为多种形态，例如某个接口具有多种实现，某个类具有多个子类，这些实现或子类就是接口或其父类的多种形态，在Java 中多态提供了一个很有用的特性：父类类型的变量可以存储子类对象的引用.</p><p>例如，对于一个子类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span> <span class="keyword">extends</span> <span class="title">People</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>则可以使用<code>People</code>类型的变量来存储<code>Student</code>类型的对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">People student = <span class="keyword">new</span> Student();</span><br></pre></td></tr></table></figure><p>同理，在定义方法的参数列表时，也可以充分利用多态带来的灵活性，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tools</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">showInfo</span><span class="params">(People people)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"name:"</span>+people.getName()+<span class="string">"\nage"</span>+people.getAge());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用<code>showInfo()</code>方法时，可以传入<code>People</code>的子类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Tools.showInfo(<span class="function">New <span class="title">Student</span><span class="params">()</span>)</span>;</span><br></pre></td></tr></table></figure><p>如果<code>Student</code>类中重写了父类的方法，虽然<code>Student</code>对象的引用存储在其父类类型的变量中，但实际调用时，还是调用的<code>Student</code>类中的方法，因为在调用方法时，调用的主体会作为隐式参数传递给该方法(<strong>this</strong>).</p><p>多态性质很灵活实用，但依然存在限制，例如，当需要调用子类特有的方法时，需要将类型强制转换为子类类型，才可以调用子类中特有的方法，例如对于这样的父类与子类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Father</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">showInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"father"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Child</span> <span class="keyword">extends</span> <span class="title">Father</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">showInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Child"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">extraFunc</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"extraFunc"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>利用多态性质可以这样定义和存储对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Father father = <span class="keyword">new</span> Child();</span><br></pre></td></tr></table></figure><p>但这样只能调用<code>Father</code>类中的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">father.showInfo();<span class="comment">// 输出 Child</span></span><br><span class="line">father.extraFunc();  <span class="comment">// 编译错误，找不到该方法</span></span><br></pre></td></tr></table></figure><p>这是因为编译器会根据当前类型来生成其可以调用的方法列表，此时<code>Child</code>对象存储在<code>Father</code>类型变量中，则只能查找<code>Father</code>类的方法列表，要想调用<code>Child</code>类中的方法，必须将其强制转换为<code>Child</code>类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Child child = (Child)father;</span><br><span class="line">child.extraFunc();</span><br><span class="line"><span class="comment">// 或者</span></span><br><span class="line">((Child)father).extraFunc();</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;类的继承&quot;&gt;&lt;a href=&quot;#类的继承&quot; class=&quot;headerlink&quot; title=&quot;类的继承&quot;&gt;&lt;/a&gt;类的继承&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;Java中可以通过继承来扩展一个类的功能，复用现有的方法，Java中使用&lt;code&gt;extends&lt;/code&gt;关键字来进行继承，被继承的类称为父类，继承了其它类的类称为子类.&lt;/p&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>Java类与对象</title>
    <link href="http://zylyye.cn/2018/05/04/java-base/java-class-and-object/"/>
    <id>http://zylyye.cn/2018/05/04/java-base/java-class-and-object/</id>
    <published>2018-05-04T07:44:58.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="定义简单类"><a href="#定义简单类" class="headerlink" title="定义简单类"></a>定义简单类</h2><hr><p>Java中使用class关键字来定义一个类，所有的变量及方法必须定义在类中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><a id="more"></a><p>Java中每个源文件中可以包含多个类，但一个类中只能有一个public类，对编译器来说，一个源文件是一个编译单元，只有public类可以在外部可见，其他非public类只能在本包中可见，及每个编译单元只对外提供一个接口，编译后，每个类生成一个class文件，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Demo.java 文件中的内容</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Other</span> </span>&#123;</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于像<em>Demo.java</em>这样一个包含多个Class的源文件，当使用<code>javac</code>命令进行编译时，会产生多个class文件。</p><p>如果想要运行这个类，必须在类中定义一个<code>main</code>方法:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>main</code>方法是一个静态方法，任何类都可以定义一个main方法，同一个文件中的每个类中也可以同时都定义各自的main方法，但一次只会调用一个类中的main方法(取决于你运行哪个class文件).</p><p>通常一个简单可运行 的Java源文件中应给这么定义类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"some text here"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>class前的修饰符<code>public</code>主要与该类的访问权限有关，不影响类中<code>main</code>方法的执行.</p><h2 id="类的实例化"><a href="#类的实例化" class="headerlink" title="类的实例化"></a>类的实例化</h2><hr><p>类的实例化即使用某个类来创建该类对象的过程，Java中使用new关键字进行类的实例化,如对于文章开头定义的<code>Person</code>类, 创建该类的一个对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> Person();</span><br></pre></td></tr></table></figure><p>使用<code>new</code>关键字创建一个类的对象时，会返回这个新创建对象的引用，通常需要使用该对象所属类型的变量来存储这个引用，以备后续使用：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Person person = <span class="keyword">new</span> Person();</span><br></pre></td></tr></table></figure><h2 id="类的访问控制"><a href="#类的访问控制" class="headerlink" title="类的访问控制"></a>类的访问控制</h2><hr><p>Java中的类拥有两种访问权限：</p><ul><li>package-private 默认权限(class前没有修饰符的情况)，只有本包中的类可见</li><li>public 所有包中的类可见</li></ul><p>对于类中的成员变量与成员方法，有四种访问权限：</p><ul><li>package-private 默认权限(变量或方法前无修饰符的情况)，只有本包中类可访问</li><li>public 所有包中的类可访问</li><li>protected 本包中的类以及外包的子类可以访问</li><li>private 只有本类可访问</li></ul><p><strong>默认权限package-private与protected很相似，两者的区别主要体现在当外包想要访问该成员时，拥有默认权限的成员外包无法访问，而对于protected的方法，只需继承这个方法所在的类，即可访问这个方法.</strong></p><h2 id="类的变量与方法"><a href="#类的变量与方法" class="headerlink" title="类的变量与方法"></a>类的变量与方法</h2><hr><p>一个类主要由变量与方法组成，直接定义在类中的变量与方法称为<strong>成员变量</strong>与<strong>成员方法</strong>.</p><h3 id="成员变量与局部变量"><a href="#成员变量与局部变量" class="headerlink" title="成员变量与局部变量"></a>成员变量与局部变量</h3><p>成员变量：</p><ul><li>定义在类中</li><li>在整个类中有效</li><li>实例化该类时，会自动初始化，数值型初始化为0，布尔型初始化为false，其他对象类型初始化为null；</li><li>非静态变量随着对象存在和消亡</li></ul><p>局部变量：</p><ul><li>定义在一个方法中</li><li>只在定义它的方法中有效</li><li>不会自动初始化</li><li>只在方法执行时存在</li></ul><h3 id="成员方法"><a href="#成员方法" class="headerlink" title="成员方法"></a>成员方法</h3><p>成员方法即直接定义在类中的方法，通常用来访问或修改对象中的数据，一个方法的定义包含<strong>访问修饰符</strong>(可选)，<strong>返回值类型</strong>(没有则为void)，<strong>方法名</strong>，以及括号里要接收的<strong>参数列表</strong>，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>类中的<code>setName</code>方法定义了一个<code>String</code>类型的参数<code>name</code>，该参数称为显式参数，该方法被调用时，还会传递一个隐式参数，如在方法中的关键字this即表示传递进来的隐式参数，它指代方法所处对象.</strong></p><h3 id="方法的可变参数"><a href="#方法的可变参数" class="headerlink" title="方法的可变参数"></a>方法的可变参数</h3><p>定义一个方法时，可以声明一个可变的参数列表：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span><span class="params">(<span class="keyword">int</span>... arr)</span></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(arr.length &gt; <span class="number">0</span>)&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i:arr)&#123;</span><br><span class="line">            System.out.println(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>调用该方法时，传入的参数会自动被包装成指定类型的数组.</p><h3 id="静态变量与静态方法"><a href="#静态变量与静态方法" class="headerlink" title="静态变量与静态方法"></a>静态变量与静态方法</h3><p>要想访问类中的成员变量及成员方法，必须先实例化该类，然后再通过这个对象访问其中的成员，但如果将变量与方法定义为静态的，并且外部可访问，则可以直接通过类名访问，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        Other.count++;</span><br><span class="line">        Other.func();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Other</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">func</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// some statements</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>静态变量与方法的特点：</p><ul><li>无须实例化类即可访问，也可以使用该类的对象访问</li><li>该类所有的对象共享同一个静态变量或方法</li><li>静态方法只能访问静态变量或方法，要想访问非静态成员，必须创建相应的对象</li></ul><p>除了静态变量，与之类似，也可以定义静态常量.</p><h3 id="方法重载"><a href="#方法重载" class="headerlink" title="方法重载"></a>方法重载</h3><p>在定义方法时，可以定义多个同名的方法（包括静态方法)，这些方法通过接收不同的参数来相互区分，调用时，编译器会根据传入的参数，自动的选择相匹配的方法，这个过程称为<strong>重载解析</strong>.</p><p>因此，在定义同名的方法时，只需保证参数列表不同，方法的返回类型可以不同，例如以下的定义时合法的：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">get</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> age;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">get</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="构造方法"><a href="#构造方法" class="headerlink" title="构造方法"></a>构造方法</h2><hr><p>类中的构造方法会在实例化该类时自动调用，可以自定义构造方法，如果没有自定义构造方法，编译器会为类生成一个空参数列表的构造方法.</p><h3 id="定义构造方法"><a href="#定义构造方法" class="headerlink" title="定义构造方法"></a>定义构造方法</h3><p>与方法类似，但构造方法没有返回类型，并且构造方法名必须与类名相同：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Other</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> count;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Other</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">var</span> = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>与方法一样，构造方法也可以重载：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Other</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Other</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 可以不用手动将该变量初始化为，成员变量会自动被初始化为0</span></span><br><span class="line">        count = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Other</span><span class="params">(<span class="keyword">int</span> count)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.count = count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这样在创建该类的对象时，可以将值传入该构造函数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Other other = <span class="keyword">new</span> Other(<span class="number">9</span>);</span><br></pre></td></tr></table></figure><h3 id="调用其它构造方法"><a href="#调用其它构造方法" class="headerlink" title="调用其它构造方法"></a>调用其它构造方法</h3><p>有时候，为了重用其他构造方法，可以使用<strong>this</strong>手动调用类中的其他构造方法(必须在执行其他语句前先调用其他构造方法)</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Student</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 当局部变量与成员变量重名时，可以使用this来显示的区分成员变量与局部变量</span></span><br><span class="line">        <span class="keyword">this</span>.name = name;     </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Student</span><span class="params">(String name,<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 必须是第一句</span></span><br><span class="line">        <span class="keyword">this</span>(name);     </span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="初始化块"><a href="#初始化块" class="headerlink" title="初始化块"></a>初始化块</h2><hr><p>当成员变量的初始化过程比较复杂时，可以将初始化过程放在初始化块中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化块</span></span><br><span class="line">    &#123;</span><br><span class="line">        count = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于静态成员，可以使用静态初始化块：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> staticCount;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 静态初始化块</span></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        staticCount = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行顺序：</p><ol><li>当类第一次被加载时，静态初始化块执行(静态初始化块只会执行一次)</li><li>构造该类的对象时，非静态初始化块执行</li><li>构造方法执行</li></ol><h2 id="内部类"><a href="#内部类" class="headerlink" title="内部类"></a>内部类</h2><hr><p>在一个类的内部，还可以定义类，这样的类称为内部类，包含该内部类的类称为外部类。</p><h3 id="成员内部类"><a href="#成员内部类" class="headerlink" title="成员内部类"></a>成员内部类</h3><p>成员内部类定义在一个类的内部，与类成员方法处于一个层级.</p><ul><li>定义成员内部类时，类似于定义类中的成员变量，可以使用public，protected，private修饰符，这些访问权限修饰符会控制成员内部类的可见性</li><li>成员内部类可以直接访问外部类的成员，例如：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Outer</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="comment">// 成员内部类</span></span><br><span class="line">    <span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">Inner</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// 访问Outer类的成员变量</span></span><br><span class="line">            count = <span class="number">10</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Inner inner = <span class="keyword">new</span> Inner();</span><br><span class="line">        inner.init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当调用<code>Outer</code>对象中的<code>setCount()</code>方法时，内部类<code>Inner</code>对象会被创建，<code>Inner</code>对象的<code>init()</code>方法可以轻易地更改外部类的私有成员<code>count</code>.</p><ul><li>如果内部类中含有与外部类同名的成员，这样直接访问的方式只会访问内部类自己的成员，此时可以借助<strong>OuterClass.this</strong>来显示访问外部类的成员，如：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Outer</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Inner</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(<span class="keyword">int</span> count)</span> </span>&#123;</span><br><span class="line">            Outer.<span class="keyword">this</span>.count = count;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        Inner inner = <span class="keyword">new</span> Inner();</span><br><span class="line">        inner.init(<span class="number">10</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>如果成员内部类在外部可见，可以在其它类中创建一个内部类对象：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 其他类中</span></span><br><span class="line">Outer.Inner inner = <span class="keyword">new</span> Outer().new Inner();</span><br></pre></td></tr></table></figure><p>由于成员内部类不是静态的，因此必须创建一个外部类的对象，再创建内部类对象.</p><h3 id="静态内部类"><a href="#静态内部类" class="headerlink" title="静态内部类"></a>静态内部类</h3><p>可以在类中使用<code>static</code>关键字定义一个静态内部类。</p><ul><li>与成员内部类不同，静态内部类中可以定义静态变量或方法；</li><li>类似于静态方法，在其它类中创建一个静态内部类的对象，不需要先创建其外部类的对象：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Outer</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Inner</span></span>&#123;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在其它类中，可以直接创建一个Inner对象：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Outer.Inner inner = <span class="keyword">new</span> Outer.Inner();</span><br></pre></td></tr></table></figure><ul><li>在接口中定义的成员内部类会被转变为静态内部类</li></ul><h3 id="局部内部类"><a href="#局部内部类" class="headerlink" title="局部内部类"></a>局部内部类</h3><p>定义在局部作用域的类即为局部作用域(如定义在一个方法中的类).</p><ul><li>局部内部类不能使用访问权限修饰符(public,protected,private);</li><li>局部内部类不仅可以访问外部类的成员，还可以访问局部变量，但是，在内部类中不能改变该变量，否则编译器会报错(相当于要求该局部变量是常量):</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Outer</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">10</span>;</span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">Inner</span></span>&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line">            <span class="comment">// 编译报错</span></span><br><span class="line">                count++; </span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">new</span> Inner().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是可以将该变量保存在一个一维数组中，这样指向这个数组的变量不会发生改变，但可以改变数组中的元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Outer</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] count = &#123;<span class="number">10</span>&#125;;</span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">Inner</span></span>&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line">                count[<span class="number">0</span>]++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">new</span> Inner().init();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="匿名内部类"><a href="#匿名内部类" class="headerlink" title="匿名内部类"></a>匿名内部类</h3><p>匿名内部类用于直接创建一个类的子类对象，其创建语法如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> SuperType &#123;</span><br><span class="line">    <span class="comment">// Override some methods</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在后面的花括号中，可以覆盖父类的方法.</p><p>匿名内部类没有构造函数.</p><p>如果在局部域中创建一个匿名内部类，则这是一个匿名局部内部类，其遵守局部内部类的规则，如果在外部类中创建匿名内部类，则这是匿名成员内部类，遵守成员内部类规则.</p><p>例如，通常在给图形界面绑定事件时，经常使用匿名内部类：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String... args)</span></span>&#123;</span><br><span class="line">        JFrame frame = <span class="keyword">new</span> JFrame();  <span class="comment">// 创建一个窗体框架</span></span><br><span class="line">        frame.setSize(<span class="number">800</span>,<span class="number">600</span>);       <span class="comment">// 设置窗体大小</span></span><br><span class="line">        frame.addWindowListener(<span class="keyword">new</span> WindowAdapter() &#123;    <span class="comment">// 添加事件</span></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">windowClosing</span><span class="params">(WindowEvent e)</span> </span>&#123;</span><br><span class="line">                <span class="comment">//do something</span></span><br><span class="line">                System.exit(<span class="number">0</span>);    <span class="comment">// 退出程序</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        frame.setVisible(<span class="keyword">true</span>);     <span class="comment">// 设置窗体可见</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="包"><a href="#包" class="headerlink" title="包"></a>包</h2><hr><p>Java中的包是用来组织和管理类，类似于其他语言中的命名空间与文件系统的文件夹，使用包能更加有条理的管理与归类Java中的类，同时可以避免同名类冲突.</p><h3 id="定义包"><a href="#定义包" class="headerlink" title="定义包"></a>定义包</h3><p>在Java文件中，使用关键字<code>package</code>定义当前文件中的类属于哪个包，包定义在除注释外的第一行：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.tools;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span>...// 文件中定义多个类均属于该包</span></span><br></pre></td></tr></table></figure><h3 id="导入包中的类"><a href="#导入包中的类" class="headerlink" title="导入包中的类"></a>导入包中的类</h3><p>如果需要使用刚才定义在<code>com.example.tools</code>包中的<code>Demo</code>类，需要在文件顶部使用关键字import进行导入：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> com.example.tools.Demo;</span><br></pre></td></tr></table></figure><p>不止可以导入类，还可以使用<code>import static</code>导入静态成员或方法，如<code>out</code>是<code>System</code>类中的静态成员，通常使用<code>out</code>的成员方法在控制台输出文本:</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">System.out.println(<span class="string">"some text here"</span>);</span><br></pre></td></tr></table></figure><p>可以静态导入out来方便的调用该方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.lang.System.out;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo</span></span>&#123;</span><br><span class="line">    out.println(<span class="string">"some text here"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;定义简单类&quot;&gt;&lt;a href=&quot;#定义简单类&quot; class=&quot;headerlink&quot; title=&quot;定义简单类&quot;&gt;&lt;/a&gt;定义简单类&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;Java中使用class关键字来定义一个类，所有的变量及方法必须定义在类中：&lt;/p&gt;
&lt;figure class=&quot;highlight java&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;2&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;3&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;4&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;&lt;span class=&quot;class&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;class&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;Person&lt;/span&gt; &lt;/span&gt;&amp;#123;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;private&lt;/span&gt; String name;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;    &lt;span class=&quot;keyword&quot;&gt;private&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;int&lt;/span&gt; age;&lt;/span&gt;&lt;br&gt;&lt;span class=&quot;line&quot;&gt;&amp;#125;&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
  </entry>
  
  <entry>
    <title>Java类型与变量</title>
    <link href="http://zylyye.cn/2018/04/19/java-base/java-type-and-variable/"/>
    <id>http://zylyye.cn/2018/04/19/java-base/java-type-and-variable/</id>
    <published>2018-04-18T16:35:24.000Z</published>
    <updated>2020-10-07T14:07:50.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="Java中的基本类型"><a href="#Java中的基本类型" class="headerlink" title="Java中的基本类型"></a>Java中的基本类型</h2><hr><p>Java中共有8中基本类型：</p><ul><li>整型：byte, short, int, long</li><li>浮点型：float, double</li><li>字符型：char</li><li>布尔型：boolean</li></ul><a id="more"></a><h3 id="整型"><a href="#整型" class="headerlink" title="整型"></a>整型</h3><hr><p>Java中没有无符号整型，所有的数据类型存储位数也与平台无关，整型存储位数与范围如下：</p><table><thead><tr><th align="center">类型</th><th align="center">存储字节数</th><th align="center">范围</th></tr></thead><tbody><tr><td align="center">byte</td><td align="center">1字节</td><td align="center">-2^7 ~ 2^7-1</td></tr><tr><td align="center">short</td><td align="center">2字节</td><td align="center">-2^15 ~ 2^15-1</td></tr><tr><td align="center">int</td><td align="center">4字节</td><td align="center">-2^31 ~ 2^31-1</td></tr><tr><td align="center">long</td><td align="center">8字节</td><td align="center">-2^63 ~ 2^63-1</td></tr></tbody></table><p>其中byte类型类似于C中的char类型，Java程序中可以使用十进制，八进制，十六进制以及二进制的形式来表示这些数值：</p><ul><li>八进制：以数字0开头，如 <code>017</code></li><li>十六进制：以0x或0X开头，如 <code>0X4a</code></li><li>二进制(自Java7开始)：以0b或0B开头，如 <code>0B1011</code>，还可以用下划线来进行分割，以便更好的阅读，编译器会自动去除下划线，如 <code>0B1001_0011</code></li></ul><p><strong>此外，在表示长整型数值时，应当在数值后添加一字母l或L，来显示的表明该数值为long类型.</strong></p><h3 id="浮点型"><a href="#浮点型" class="headerlink" title="浮点型"></a>浮点型</h3><hr><p>浮点类型及存储位数：</p><table><thead><tr><th align="center">类型</th><th align="center">存储字节数</th><th align="center">范围</th></tr></thead><tbody><tr><td align="center">float</td><td align="center">4字节</td><td align="center">大约 ± 3.402 823 47E+38F (有效位数为 6 ~ 7 位）</td></tr><tr><td align="center">double</td><td align="center">8字节</td><td align="center">大约 ± 1.797 693 134 862 315 70E+308 (有效位数为 15 位）</td></tr></tbody></table><p>浮点数可以使用带小数点的小数如：<code>3.14</code>，或科学计数法，如: <code>3.1412E2</code>,以及十六进制，如：<code>0x4ap-3</code>,来表示，在十六进制表示法中，p用来分隔尾数(4a)与指数(-3),且尾数用十六进制表示，而指数用十进制表示，并以2为基数，例如 0x4ap-3=(4<em>16+10)</em>2^-3.</p><p><strong>在表示float类型是，应在数值后添加字母f或F来显示的表明该数值为float类型，否则Java默认将该数值当做double型来处理(double类型数值后也可添加字母d或D).</strong></p><h3 id="字符型char"><a href="#字符型char" class="headerlink" title="字符型char"></a>字符型char</h3><hr><p>Java中的char类型原本设计用来存储单个字符，char变量中存储字符时采用UTF-16编码格式，UTF-16一个代码单元占用16位，可以用来存储大多数unicode编码中的字符，但对于其他的字符，则需要至少2个UTF-16代码单元，因此，char变量并不能表示一个字符，其真是的含义是表示一个UTF-16代码单元，因此char变量的含义如下：</p><ul><li>代表一个UTF-16代码单元</li><li>占用2字节</li></ul><h3 id="布尔型boolean"><a href="#布尔型boolean" class="headerlink" title="布尔型boolean"></a>布尔型boolean</h3><hr><p>Java中用true与false表示布尔型，且布尔型与其他类型不能相互转换。</p><h3 id="字符串String"><a href="#字符串String" class="headerlink" title="字符串String"></a>字符串String</h3><hr><p>Java中提供了一个预定义类<strong>String</strong>用来表示字符串，字符串之间可以使用加号(+)进行拼接，如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">"hello"</span> + <span class="string">"java"</span>;</span><br></pre></td></tr></table></figure><p>String类中没有提供修改当前字符串的方法。</p><h4 id="字符串与编码"><a href="#字符串与编码" class="headerlink" title="字符串与编码"></a>字符串与编码</h4><p>Java使用unicode编码，String类中使用一个char数组来保存字符串，这个char数组使用了UTF-16编码格式来保存unicode字符。</p><h4 id="获取字符串的长度"><a href="#获取字符串的长度" class="headerlink" title="获取字符串的长度"></a>获取字符串的长度</h4><p>String中提供了获取字符串长度的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">length</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure><p>而该方法只是简单的返回内char数组的长度，因此，该方法实际返回的是该字符串使用UTF-16编码格式编码后的代码单元数量，由于大多数字符使用UTF-16格式编码都只产生一个代码单元，因此该方法一般不会产生问题，但不够严谨。</p><p>一串字符串的字符数应该使用字符串中unicode码点数(code point)来表示，因此，应该这样来获取字符串的长度：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">String str = <span class="string">"Hello World"</span>;</span><br><span class="line"><span class="keyword">int</span> length = str.codePointCount(<span class="number">0</span>, str.length());</span><br><span class="line"><span class="comment">// 或者</span></span><br><span class="line">length = str.codePoints().toArray().length;</span><br></pre></td></tr></table></figure><h4 id="判断字符串相等"><a href="#判断字符串相等" class="headerlink" title="判断字符串相等"></a>判断字符串相等</h4><p>不能依赖运算符<code>==</code>来判断两个字符串是否相等，因为字符串是一个对象，两个相同的字符串可能是两个不同的对象，而使用<code>==</code>来比较两个对象时，实际比较的是这两个对象的地址是否相等。</p><p>String对象中提供了用于比较字符串是否相等的方法：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span></span>;</span><br></pre></td></tr></table></figure><h2 id="变量与常量"><a href="#变量与常量" class="headerlink" title="变量与常量"></a>变量与常量</h2><hr><p>Java是强类型编程语言，所有的变量在使用前都必须先进行声明和初始化。</p><h3 id="变量声明"><a href="#变量声明" class="headerlink" title="变量声明"></a>变量声明</h3><hr><p>声明一个变量时需指定变量的类型与变量名，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> num;</span><br><span class="line"><span class="keyword">double</span> radius;</span><br><span class="line"><span class="keyword">boolean</span> isMale;</span><br><span class="line">String name;</span><br></pre></td></tr></table></figure><h3 id="变量的初始化"><a href="#变量的初始化" class="headerlink" title="变量的初始化"></a>变量的初始化</h3><hr><p>变量可以在声明的同时进行初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">String str = <span class="string">"this is a string"</span>;</span><br></pre></td></tr></table></figure><p>也可以在声明之后再初始化，但在使用该变量前，必须先进行初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">String str;</span><br><span class="line">str = <span class="string">"this is a string"</span>;</span><br></pre></td></tr></table></figure><h3 id="常量"><a href="#常量" class="headerlink" title="常量"></a>常量</h3><hr><p>与变量类似，常量使用final关键字进行声明，常量在进行初始化时，也可以先声明，以后再初始化，但只能初始化一次：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">final</span> <span class="keyword">double</span> PI = <span class="number">3.14125</span>;</span><br><span class="line"><span class="comment">//等同于</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">double</span> PI;</span><br><span class="line">PI = <span class="number">3.14125</span>;</span><br></pre></td></tr></table></figure><h3 id="变量中的引用与值"><a href="#变量中的引用与值" class="headerlink" title="变量中的引用与值"></a>变量中的引用与值</h3><hr><p>对于基本类型变量，该变量存储的是相应类型的值，而对于对象变量，该变量存储的是相应对象的引用，在Java中，传递变量或将变量赋值给其他变量时，总是会复制变量存储的内容来进行传递，对于基本类型变量，总是会复制变量的值，而对于对象变量，只是单纯的复制了存储在变量中的引用，因此在传递变量给方法时，会有<strong>值传递与引用传递</strong>，而其本质都是传递存储在变量中的内容。</p><h2 id="大数值类"><a href="#大数值类" class="headerlink" title="大数值类"></a>大数值类</h2><hr><p>基本的数值类型表示的数值范围有限，在java.math包中定义了两个分别表示整型和浮点型的大数值类：BigInteger与BigDecimal，它们可以表示任意精度的数值，并且在类中提供了相应的算数运算方法。</p><h2 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h2><hr><p>Java中的数组可以是原始类型数组，也可以是对象数组。</p><h3 id="数组的声明与初始化"><a href="#数组的声明与初始化" class="headerlink" title="数组的声明与初始化"></a>数组的声明与初始化</h3><hr><p>声明数组时需要指定数组的类型，大小，数组名：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">10</span>];</span><br><span class="line"><span class="comment">// 或者</span></span><br><span class="line"><span class="keyword">int</span> arr[] = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">10</span>];</span><br></pre></td></tr></table></figure><p>也可以在声明的同时进行初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[] arr = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line"><span class="comment">//相当于</span></span><br><span class="line"><span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">3</span>];</span><br><span class="line">arr[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">arr[<span class="number">1</span>] = <span class="number">2</span>;</span><br><span class="line">arr[<span class="number">2</span>] = <span class="number">3</span>;</span><br></pre></td></tr></table></figure><p>还可以声明一个匿名数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line"><span class="comment">//将匿名数组存储给一个变量</span></span><br><span class="line"><span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br></pre></td></tr></table></figure><h3 id="多维数组"><a href="#多维数组" class="headerlink" title="多维数组"></a>多维数组</h3><hr><p>例如声明一个二维数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[][] arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>][<span class="number">3</span>];</span><br></pre></td></tr></table></figure><p>在声明的同时进行初始化：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[][] a = &#123;</span><br><span class="line">                &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;,</span><br><span class="line">                &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;,</span><br><span class="line">            &#125;;</span><br></pre></td></tr></table></figure><h3 id="不规则数组"><a href="#不规则数组" class="headerlink" title="不规则数组"></a>不规则数组</h3><hr><p>实际上Java只支持一维数组，当一维数组中的元素存储另一个数组时，即成为了多维数组，因此，可以创建不规则的多维数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[][] arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">3</span>][];</span><br><span class="line">arr[<span class="number">0</span>] = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">4</span>];</span><br><span class="line">arr[<span class="number">1</span>] = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">5</span>];</span><br><span class="line">arr[<span class="number">2</span>] = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>];</span><br></pre></td></tr></table></figure><h3 id="数组的遍历"><a href="#数组的遍历" class="headerlink" title="数组的遍历"></a>数组的遍历</h3><hr><p>数组拥有length属性表示创建数组时的大小，利用该值可合法的遍历数组中的所有元素：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>,i&lt;arr.length;i++)&#123;</span><br><span class="line">    arr[i]++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果只需要访问数组中的元素，可以使用for each循环：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> item:arr)&#123;</span><br><span class="line">    System.out.println(item);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="数组的拷贝"><a href="#数组的拷贝" class="headerlink" title="数组的拷贝"></a>数组的拷贝</h3><hr><p>Java中一个数组变量存储的只是一个数组的引用，因此：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[] arr = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;</span><br><span class="line"><span class="keyword">int</span>[] arrCopy = arr;</span><br></pre></td></tr></table></figure><p>像这样的只是拷贝了数组的引用，arr与arrCopy依然是一个数组，最通用的方法是遍历第一个数组中的所有元素并将它复制给第二个数组：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span>[] arrCopy = <span class="keyword">new</span> <span class="keyword">int</span>[arr.length];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;arr.length;i++)&#123;</span><br><span class="line">    arrCopy[i] = arr[i];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>不过Java中提供了几个方法用于这样的数组拷贝：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">native</span> <span class="keyword">void</span> System.arraycopy(Object src, <span class="keyword">int</span> srcPos, Object dest, <span class="keyword">int</span> destPos, <span class="keyword">int</span> length);</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span>[] Arrays.copyOf(<span class="keyword">int</span>[] src, <span class="keyword">int</span> newLength);</span><br><span class="line">Object.clone();</span><br></pre></td></tr></table></figure><p>第一个拷贝方法必须先初始化目标数组；</p><p>对于第二个方法，Arrays类中，重载了所有基本类型的数组拷贝方法，对于对象数组的拷贝，还实现了泛型copyOf方法；</p><p>第三个拷贝方法是数组类重写了父类Object的clone()方法；</p><p>不过这些拷贝方法都只是浅拷贝，对于基本类型数组，不会产生问题，但对于对象数组，拷贝数组元素时，也只是复制了元素所存储的引用，因此，使用这些方法拷贝对象数组时，它们的元素依然指向相同的对象，这时可以遍历原始数组，分别对每个对象进行复制。</p><h2 id="枚举类型"><a href="#枚举类型" class="headerlink" title="枚举类型"></a>枚举类型</h2><hr><p>对于那些只有有限个可能值的集合，可以使用枚举类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 定义枚举类</span></span><br><span class="line"><span class="keyword">enum</span> Color&#123;RED, GREEN, BLUE&#125;;</span><br><span class="line"><span class="comment">// 使用枚举</span></span><br><span class="line">Color blue = Color.BLUE;</span><br></pre></td></tr></table></figure><h2 id="Java包装类"><a href="#Java包装类" class="headerlink" title="Java包装类"></a>Java包装类</h2><hr><p>Java中，基本类型不是对象，没有自己的方法，为了让这些基本类型拥有面向对象的特性，Java中为基本类型提供了包装类：</p><table><thead><tr><th align="center">基本类型</th><th align="center">包装类</th></tr></thead><tbody><tr><td align="center">byte</td><td align="center">Byte</td></tr><tr><td align="center">short</td><td align="center">Short</td></tr><tr><td align="center">int</td><td align="center">Integer</td></tr><tr><td align="center">long</td><td align="center">Long</td></tr><tr><td align="center">float</td><td align="center">Float</td></tr><tr><td align="center">double</td><td align="center">Double</td></tr><tr><td align="center">boolean</td><td align="center">Boolean</td></tr><tr><td align="center">char</td><td align="center">Character</td></tr></tbody></table><p>在每个基本类型的包装类中，提供了相应的属性(如最大值，最小值)，以及实现了许多操作对象的方法。</p><h4 id="自动装箱-拆箱"><a href="#自动装箱-拆箱" class="headerlink" title="自动装箱/拆箱"></a>自动装箱/拆箱</h4><p>将基本类型转换为相应的包装类型即成为装箱，相反的过程成为拆箱，在需要的时候，Java编译器会为我们自动的进行装箱与拆箱，自动装箱/拆箱在两种情况下会进行：</p><ul><li><strong>赋值</strong></li><li><strong>方法调用</strong></li></ul><p>如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Integer integer = <span class="number">123</span>;    <span class="comment">//自动装箱 调用Integer.valueOf(123);</span></span><br><span class="line"><span class="keyword">int</span> i = integer;          <span class="comment">//自动拆箱 调用integer.intValue();</span></span><br></pre></td></tr></table></figure><p>如上，编译器会根据需要自动的调用方法来进行装箱与拆箱，其中 valueOf(args) 是Integer累的静态方法，返回Integer对象。同样，在进行方法调用时，编译器会根据方法需要的参数类型进行自动装箱与拆箱。</p><h2 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h2><hr><h3 id="基本类型之间的强制转换"><a href="#基本类型之间的强制转换" class="headerlink" title="基本类型之间的强制转换"></a>基本类型之间的强制转换</h3><hr><p>对于基本数值类型(byte, short, int, long, float, double)，在需要时，低精度的数值类型会自动的往高精度的数值类型转换，此时无需手动转换，如果需要将高精度类型向低精度类型进行转换，可以手动进行强制转换：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">double</span> d = <span class="number">3.14</span>;</span><br><span class="line"><span class="keyword">int</span> i = (<span class="keyword">int</span>)d;   <span class="comment">//强制转换为3</span></span><br></pre></td></tr></table></figure><p>这种转换会根据转换的类型自动截断，不会进行四舍五入处理。</p><p>但布尔，字符型与数值型之间不能相互强制转换，但可以使用条件语句将布尔与数值型之间进行互转：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">boolean</span> bool = <span class="keyword">true</span>;</span><br><span class="line"><span class="keyword">int</span> i = bool? <span class="number">1</span>:<span class="number">0</span>;     <span class="comment">//使用三元运算符将布尔值转换为整型</span></span><br></pre></td></tr></table></figure><h3 id="基本类型转为对象"><a href="#基本类型转为对象" class="headerlink" title="基本类型转为对象"></a>基本类型转为对象</h3><hr><p>除了利用编译器的自动装箱机制，也可以手动的将基本类型转换为对象，如以 int 型为例：</p><ul><li>利用静态方法：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Integer integer = Integer.valueOf(<span class="number">123</span>);</span><br></pre></td></tr></table></figure><ul><li>利用构造方法：</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Integer integer = <span class="keyword">new</span> Integer(<span class="number">123</span>);</span><br></pre></td></tr></table></figure><h3 id="字符串与基本类型之间的转换"><a href="#字符串与基本类型之间的转换" class="headerlink" title="字符串与基本类型之间的转换"></a>字符串与基本类型之间的转换</h3><hr><p>每个包装类中提供了静态的 parseXXX方法用于将字符串转换为相应的基本类型，例如</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">String str = <span class="string">"123"</span>;</span><br><span class="line"><span class="keyword">int</span> i = Integer.parseInt(str);</span><br></pre></td></tr></table></figure><p>还可以制定转换基数：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> i = Integer.parseInt(<span class="string">"4a"</span>,<span class="number">16</span>);   <span class="comment">// i = 74</span></span><br></pre></td></tr></table></figure><p>注意: 如果字符串中包含其他字符会抛出NumberFormatException异常。</p><p>当需要将对象转换为字符串是，只需调用字符串中的 toString() 方法即可，许多预定义类都继承或重写了该方法，在自定义类中，可根据需要重写该方法。</p>]]></content>
    
    <summary type="html">
    
      &lt;h2 id=&quot;Java中的基本类型&quot;&gt;&lt;a href=&quot;#Java中的基本类型&quot; class=&quot;headerlink&quot; title=&quot;Java中的基本类型&quot;&gt;&lt;/a&gt;Java中的基本类型&lt;/h2&gt;&lt;hr&gt;
&lt;p&gt;Java中共有8中基本类型：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;整型：byte, short, int, long&lt;/li&gt;
&lt;li&gt;浮点型：float, double&lt;/li&gt;
&lt;li&gt;字符型：char&lt;/li&gt;
&lt;li&gt;布尔型：boolean&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="Java基础" scheme="http://zylyye.cn/categories/Java%E5%9F%BA%E7%A1%80/"/>
    
    
      <category term="Java" scheme="http://zylyye.cn/tags/Java/"/>
    
  </entry>
  
</feed>
